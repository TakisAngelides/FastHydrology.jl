var documenterSearchIndex = {"docs":
[{"category":"section","location":"examples/ArrayGrid/#ArrayGrid","page":"Plain-array grid (no Oceananigans)","text":"This example shows ArrayHydroGrid: a grid backend built on plain Arrays, with no Oceananigans dependency. It is a drop-in swap for OGRectHydroGrid – the model, state, and simulation code below is exactly what you'd write for OGRectHydroGrid (see Kazmierczak et al 2024), since all physics code is written against the grid interface rather than against either backend directly (see Package structure in the API Reference).\n\nThis is the backend to reach for when embedding FastHydrology.jl inside a host model that already manages its own plain arrays (e.g. coupling into an existing ice-sheet model), where pulling in Oceananigans just for FastHydrology's grid would be unwanted weight.\n\nWe use a small synthetic ice-sheet geometry here, rather than real data as in the other examples, since the point is to show the grid swap itself.\n\nusing FastHydrology\nusing CairoMakie\n\nT = Float64\nNx, Ny = 50, 50\nxlims, ylims = (0.0, 50_000.0), (0.0, 50_000.0) # 50 km x 50 km domain\n\nBuild the plain-array grid – same constructor signature as OGRectHydroGrid, no Oceananigans.RectilinearGrid underneath.\n\ngrid = ArrayHydroGrid(Nx, Ny, xlims, ylims; T = T)\n\nA synthetic sloped ice-sheet geometry: thickness decreasing and bed deepening from left to right, uniform basal conditions, everywhere grounded.\n\nmask    = ones(T, Nx, Ny)\nh       = [2000.0 - 15.0 * i for i in 1:Nx, j in 1:Ny]\nb       = [-200.0 - 2.0 * j for i in 1:Nx, j in 1:Ny]\nkappa   = zeros(T, Nx, Ny)                            # hard bed everywhere\nabs_v_b = fill(100.0 / (60^2 * 24 * 365.25), Nx, Ny)  # 100 m/a basal sliding speed\nA_visc  = fill(1e-24, Nx, Ny)\nmdot    = fill(1e-6, Nx, Ny)                          # basal melt rate [kg m⁻² s⁻¹]\n\nThe model and state constructors below are identical to the OGRectHydroGrid case – they only ever touch the grid through alloc_field and grid.Nx/grid.Ny/grid.dx/grid.dy, never Oceananigans directly.\n\nlongcoupwater (the stress-gradient-coupling smoothing width) has no safe default that works for every grid resolution, so it must be passed explicitly or KazmierczakHydroModel warns and falls back to 5.0 – see its docstring for how to choose a value from your own grid's resolution relative to ice thickness. 5.0 is well resolved at this grid's 1 km spacing.\n\nmodel = KazmierczakHydroModel(grid, kappa, abs_v_b, A_visc, mdot; longcoupwater = 5.0, dissipation_verbose = false)\nstate = HydroState(grid, mask, h, b)\n\nsim = SteadyStateSimulation(model, grid, state)\nrun!(sim)\n\nArrayHydroGrid fields are plain Matrix, so we mask and build plot coordinates directly instead of using the Oceananigans-specific mask_field/visualize_field(::Field)/ visualize_grid(::OGRectHydroGrid) methods used in the other examples. The underlying visualize_field(x, y, data; kwargs...) method itself is backend-agnostic.\n\nN_plot = copy(state.N) .* 1e-6 # makes N [MPa]\nN_plot[mask .!= 1] .= NaN\n\nxc = ((1:Nx) .- 0.5) .* grid.dx # cell-center coordinates\nyc = ((1:Ny) .- 0.5) .* grid.dy\n\nfig_N = visualize_field(xc, yc, N_plot; plot_title = \"Effective pressure N [MPa]\", colorrange = (0, 10))","title":"Plain-array grid (no Oceananigans)"},{"category":"section","location":"references/#Bibliography","page":"Bibliography","text":"","title":"Bibliography"},{"category":"section","location":"API_public/#API-Reference","page":"API Reference","text":"This page documents every exported name in FastHydrology.jl. It mirrors the \"Package structure\" section of the README: the package is built around four abstractions – grid, model, state, and simulation – plus the functions that operate on them. If you are looking for a narrative introduction instead, start with the Quick start example on the front page or the runnable Examples.\n\nA typical session touches these pieces in order:\n\nBuild a grid (Grid).\nBuild a model on that grid (Model).\nBuild a state on that grid (State).\nWrap model, grid, and state in a simulation and run it (Running Simulations).\n\nThe functions under Water Flux and Effective Pressure are the physics implementations that run! calls into; they are exported and documented individually because they are also useful on their own, e.g. for inspecting an intermediate quantity or for calling only part of a model's update sequence. Data Loading, Utilities, and Plotting round out the API with helpers for getting real data in and results out.","title":"API Reference"},{"category":"section","location":"API_public/#Grid","page":"API Reference","text":"An AbstractHydroGrid describes the geometry of the simulation domain (grid size and spacing) together with a small set of backend-specific operations – allocating fields, filling halo/ghost points, convolution, gradients, masked reductions, and masked overwrites – that the physics code in Water Flux and Effective Pressure is written against instead of reaching into a specific backend's internals directly. Two concrete grids are provided: OGRectHydroGrid, wrapping an Oceananigans.RectilinearGrid, and ArrayHydroGrid, built on plain Arrays with no Oceananigans dependency (see Plain-array grid). A different array/mesh backend can be supported the same way ArrayHydroGrid was, by implementing the same interface (alloc_field, fill_halo!, convolve!, minus_gradient_x!, minus_gradient_y!, masked_mean, masked_max_abs, masked_max_abs_diff, overwrite_where!) for a new AbstractHydroGrid subtype, without touching any physics code – most of these already have a default implementation that works for any grid whose fields behave like plain arrays, so a new backend like ArrayHydroGrid only needs to implement alloc_field.","title":"Grid"},{"category":"section","location":"API_public/#Model","page":"API Reference","text":"An AbstractHydroModel holds everything specific to one subglacial hydrology parameterization: physical constants and the mutable fields the model needs (geometric potential, water flux, effective pressure, etc.), all sized to match a given grid. Two models are provided:\n\nKazmierczakHydroModel – the fast, simplified model of Kazmierczak et al. 2024, which routes distributed subglacial water flux over the geometric potential and parameterizes effective pressure for both hard and soft beds. The water source feeding that routing includes, by default, the dissipation melt rate generated by the flux itself (dissipation_melt = true; the paper drops this term as negligible, but it's kept here for generality and can be turned off). It can also include a frictional-heating term tau_b*v_b (Eq. 3 of the paper) via the sliding_law keyword – see Sliding Laws. The flux-routing sweep itself has three interchangeable implementations, selected with the psi_out_algorithm keyword, and the reportable water layer thickness state.W has three selectable closures, selected with the water_thickness_algorithm keyword – see Water Flux for both. Its numerical-safety bounds (Wmin/Wmax/q_max/sigmat) default to no-op values rather than KORI-ULB's own (still available as explicit opt-ins); longcoupwater has no such no-op default and warns if left unspecified – see KazmierczakHydroModel's own docstring for the numerical-stability reasoning behind each.\nHABHydroModel – the simpler \"height above buoyancy\" parameterization from Sec. 2.1.1 of Kazmierczak et al. 2022, assuming a direct hydraulic connection to the ocean at the grounding line.\n\nBoth are steady-state: effective pressure is recomputed from the current ice geometry and melt rate on each call rather than time-stepped, since subglacial hydrology is assumed to equilibrate much faster than the ice sheet evolves.\n\nA third model, ShaktiHydroModel, wraps the separate Shakti.jl package and is genuinely time-evolving – see Running Simulations and Coupling to Shakti.jl. It's defined unconditionally (no hard dependency on Shakti), but only becomes runnable once Shakti is loaded alongside FastHydrology, since the run!/step! methods that dispatch on it live in the FastHydrologyShaktiExt package extension.","title":"Model"},{"category":"section","location":"API_public/#Sliding-Laws","page":"API Reference","text":"An AbstractSlidingLaw turns KazmierczakHydroModel's basal sliding velocity and the current effective pressure into a basal shear stress tau_b, used for the frictional-heating term tau_b*v_b in the melt rate. NoSlidingLaw (the default) and WeertmanSlidingLaw don't depend on effective pressure, so they add at most a fixed source term with no new feedback to resolve. PowerPlasticSlidingLaw and RegularizedCoulombSlidingLaw do depend on effective pressure – since effective pressure is itself downstream of the water flux that the frictional melt term feeds into, resolve_q! (see Water Flux) widens its existing Picard loop to solve for both jointly rather than nesting a second loop around the first. calc_tau_b is the plain scalar formula for each law (used in tests/diagnostics); update_tau_b! is the field-broadcast version actually used inside the model.","title":"Sliding Laws"},{"category":"section","location":"API_public/#State","page":"API Reference","text":"AbstractHydroState holds the fields that are common to every model, independent of which hydrology parameterization is in use: the inputs (grounded-ice mask, ice thickness, bedrock elevation) and the outputs every model must produce (effective pressure N and water layer thickness W). HydroState is the (currently only) concrete implementation.","title":"State"},{"category":"section","location":"API_public/#Simulation","page":"API Reference","text":"An AbstractSimulation bundles a model, a grid, and a state, and determines how they are advanced. SteadyStateSimulation re-solves for equilibrium effective pressure on each call; TimeSimulation wraps a time-evolving AbstractHydroModel (currently just ShaktiHydroModel) and dispatches run!/step! on it.","title":"Simulation"},{"category":"section","location":"API_public/#Running-Simulations","page":"API Reference","text":"run! is the single entry point users call for a SteadyStateSimulation; it dispatches to update_steady_state! for the model type held by the simulation, which in turn calls the model-specific sequence of Water Flux and Effective Pressure updates. For a TimeSimulation, run! and step! are the two entry points – run! hands the whole time loop to the model (for ShaktiHydroModel, delegating to Shakti.run!), while step! advances a single timestep, meant for a caller (e.g. a coupled ice flow model) that owns its own timestep loop and needs to update geometry between hydrology steps. Both are stubs with no methods in FastHydrology's core – the methods that make ShaktiHydroModel actually runnable are added by the FastHydrologyShaktiExt package extension once Shakti is loaded (see Coupling to Shakti.jl).","title":"Running Simulations"},{"category":"section","location":"API_public/#Output-and-Checkpointing","page":"API Reference","text":"FastHydrology's own models are steady-state (see Model) and own no internal time loop, so periodic output/checkpointing is a manually-driven utility rather than a keyword baked into run! – a caller's own loop (typically a coupled driver, e.g. an ice-flow model calling run! once per iteration) decides when to call write_output!/save_checkpoint. NetCDFOutputWriter records named fields to a NetCDF file one time-slice at a time along an unlimited time dimension, flushed to disk on every write, so a killed run keeps every slice written before the kill. save_checkpoint/load_checkpoint! separately save/ restore a full AbstractHydroState's fields (generic over any subtype via fieldnames), so a restarted driver can resume its own loop at the right step/time. See Checkpointing and periodic output on the front page for a runnable pattern, and AbstractOutputWriter's docstring for why TimeSimulation's only model (ShaktiHydroModel) doesn't need this – Shakti.run! already has its own equivalent machinery.","title":"Output & Checkpointing"},{"category":"section","location":"API_public/#Water-Flux","page":"API Reference","text":"Functions specific to KazmierczakHydroModel that compute the geometric potential, route distributed water flux over it, and derive the water layer thickness. update_q! is the entry point; the remaining functions are its documented sub-steps, exported so each stage can be inspected or invoked independently. See update_q!'s docstring for the full algorithm description and references, including how the dissipation melt term (on by default, see Model) is resolved via Picard iteration since it depends on q, which is itself the routing algorithm's output, and how an effective-pressure-dependent sliding law (see Sliding Laws) widens that same loop to solve for q and N jointly.\n\nThe psi_out sweep itself – how much water flows out of each grid cell, following Le Brocq et al. 2009's flow-routing algorithm – has three implementations selected via model.psi_out_algorithm (AbstractPsiOutAlgorithm, set with the psi_out_algorithm keyword on KazmierczakHydroModel), which route_psi_out! dispatches on each sweep. RecursivePsiOut (accumulate_psi_out!/update_psi_out!, the default) and IterativePsiOut (update_psi_out_iterative!, a stack-based rewrite) produce identical results, verified to match bit-for-bit (including max_psi_out_calls cutoff behaviour) on synthetic grids and on real Thwaites and pan-Antarctica datasets – RecursivePsiOut is substantially faster (~30-40x, benchmarked on Thwaites 2km and pan-Antarctica 8km) but recurses as deep as the domain's longest flow-routing chain, which on real (non-toy) grids can exceed the calling process's stack size and crash with a StackOverflowError – independent of, and potentially before, max_psi_out_calls ever binds; IterativePsiOut has no such requirement (its \"stack\" is a heap-allocated Vector) at the cost of that slowdown, so prefer it for large/convoluted domains or when running under a constrained or unknown stack limit (CI, HPC job schedulers, etc.). TopologicalPsiOut is a different algorithm, not just a different traversal of the same one: a single-pass topological sort (Kahn's algorithm) over the flow-direction dependency graph, exact only when that graph is acyclic. It is on every idealized synthetic test grid in this package's own test suite, but confirmed not reliably so on real ice-sheet data (checked against a real Thwaites-2km dataset, where cycles occur regardless of the longcoupwater smoothing setting) – so a detected cycle throws an error by default rather than silently returning a corrupted field; its allow_cycles = true field opts into the old \"locally truncated but finite\" behaviour instead. See AbstractPsiOutAlgorithm's docstring for the full story.\n\nThe reportable water layer thickness state.W similarly has three selectable closures via model.water_thickness_algorithm (AbstractWaterThicknessAlgorithm, set with the water_thickness_algorithm keyword), dispatched by update_W!. state.W is documented as an areal, grid-cell-averaged quantity, so K24's own local conduit depth model.H (already computed for N_inf by update_H!) is deliberately not one of the options – it answers a different question and isn't comparable to the others; use model.H directly if you want it. DarcyWeisbachThickness (the default) inverts the turbulent parallel-plate Darcy-Weisbach closure for a wide slot – the turbulent analogue of the laminar Le Brocq/Weertman closure, and the one consistent with K24's own turbulent-flow assumption for q. ArealConduitThickness is the conduit cross-section S_inf smeared over the inter-conduit spacing l_c, comparable to a sheet model's W the way DarcyWeisbachThickness is, but bed-type independent and unclamped. LaminarThickness is the original closure (Le Brocq et al. 2009 Eq. 2), kept for direct comparison against Kori-ULB's/SHAKTI's own laminar sheet closure, but physically inconsistent with K24's turbulent-flow assumption and not recommended as \"the\" reported thickness. Both DarcyWeisbachThickness and LaminarThickness additionally take a gradient_convention (AbstractGradientConvention): the local, per-cell potential gradient (LocalGradient) or a single domain-wide masked mean of it (MeanGradient, the default for both – matching Kori-ULB's own SubWaterFlux.m averaging convention, which LaminarThickness inherits by construction and DarcyWeisbachThickness matches for consistency between the two).","title":"Water Flux"},{"category":"section","location":"API_public/#Effective-Pressure","page":"API Reference","text":"Functions that compute effective pressure N, the quantity that couples subglacial water to basal sliding. update_N! is defined for both models (with different physics in each case); the remaining functions are shared or model-specific sub-steps used to build it up.\n\nKazmierczakHydroModel's update_N_inf!/update_H! additionally select which opening terms and effective Q_c to use via model.drainage_mode (AbstractDrainageMode, set with the drainage_mode keyword): BothDrainage (the default) leaves the model free to switch between efficient and inefficient drainage automatically; EfficientOnly/ InefficientOnly force it to one regime throughout, reproducing Sect. 3.2 of Kazmierczak et al 2024.","title":"Effective Pressure"},{"category":"section","location":"API_public/#Data-Loading","page":"API Reference","text":"Loaders that read real glaciological datasets into the plain arrays expected by the Grid, Model, and State constructors above, saving users from having to hand-write that plumbing for the two data formats FastHydrology.jl has been used with so far.","title":"Data Loading"},{"category":"section","location":"API_public/#Utilities","page":"API Reference","text":"Small unit-conversion and grid-geometry helpers used throughout the package and by the data loaders above.","title":"Utilities"},{"category":"section","location":"API_public/#Plotting","page":"API Reference","text":"Convenience wrappers around CairoMakie.jl for visualizing fields and grid geometry, primarily intended for interactive exploration and for generating the figures in the Examples. visualize_field/visualize_grid are defined unconditionally (no hard dependency on CairoMakie), but only become usable once a Makie backend is loaded alongside FastHydrology, since their implementations live in the FastHydrologyMakieExt package extension – mask_field needs no plotting library and works regardless.","title":"Plotting"},{"category":"type","location":"API_public/#FastHydrology.AbstractHydroGrid","page":"API Reference","text":"Abstract type for the grid of a simulation.\n\n\n\n\n\n","title":"FastHydrology.AbstractHydroGrid"},{"category":"type","location":"API_public/#FastHydrology.OGRectHydroGrid","page":"API Reference","text":"A struct for the Oceananigans Rectilinear grid. Nx, Ny, dx, dy are cached at construction time (grid geometry never changes afterwards) so callers can read them directly, e.g. grid.dx, rather than going through an accessor function.\n\nconv_cache holds FFT plans/buffers for convolve! (see fft_convolution.jl), reused across calls since the same (image size, kernel size) combination repeats every timestep of a coupled simulation. It's a Ref rather than a genuinely mutable struct field so OGRectHydroGrid itself can stay immutable; its contents are lazily built on first use and rebuilt only if the kernel size changes.\n\n\n\n\n\n","title":"FastHydrology.OGRectHydroGrid"},{"category":"type","location":"API_public/#FastHydrology.ArrayHydroGrid","page":"API Reference","text":"A grid backed by plain Arrays – no Oceananigans dependency. Nx, Ny, dx, dy are cached at construction time, same as OGRectHydroGrid, so generic code can read them directly.\n\nconv_cache holds FFT plans/buffers for convolve! (see fftconvolution.jl), same role and same lazy-build-on-first-use behaviour as OGRectHydroGrid's own `convcache-- see its docstring.cachedfftconvolve!is grid-agnostic (itsdest/src/kernelarguments are plainAbstractMatrix, nothing Oceananigans-specific), soArrayHydroGridreuses it directly instead of falling back to the genericconvolve!default below (which callsImageFiltering.imfilter!` – confirmed by profiling to allocate its FFT plans and padded/complex buffers fresh on every call, ~20 MB on a realistic grid, since it exposes no hook to reuse them across the repeated calls one coupled simulation makes).\n\nNeeds no other overrides of the grid interface beyond alloc_field: fill_halo! is a no-op (fields carry no separate halo storage), and the default plain-array implementations of masked_mean, minus_gradient_x!/minus_gradient_y!, etc. already operate correctly on the Arrays this type allocates.\n\n\n\n\n\n","title":"FastHydrology.ArrayHydroGrid"},{"category":"function","location":"API_public/#FastHydrology.alloc_field","page":"API Reference","text":"alloc_field(\n    grid::AbstractHydroGrid\n) -> Union{Nothing, Oceananigans.Fields.Field{Oceananigans.Grids.Center, Oceananigans.Grids.Center, Oceananigans.Grids.Center, Nothing, G, Tuple{Colon, Colon, Colon}, D, T, B, Nothing} where {G, D, T, B}}\n\n\nAllocate a scalar cell-centred field on grid, initialised to zero. This is the only place that knows about the underlying field type (e.g. Oceananigans CenterField).\n\n\n\n\n\nalloc_field(grid::AbstractHydroGrid, data) -> Any\n\n\nAllocate a scalar cell-centred field on grid and initialise it from data.\n\n\n\n\n\n","title":"FastHydrology.alloc_field"},{"category":"function","location":"API_public/#FastHydrology.fill_halo!","page":"API Reference","text":"fill_halo!(field, grid::AbstractHydroGrid) -> Any\n\n\nFill ghost/halo points of field according to the boundary conditions encoded in grid.\n\nThe default here is a no-op: plain arrays carry no explicit halo storage, and grid backends built on them (unlike OGRectHydroGrid) are expected to embed boundary handling directly into their operators instead (e.g. the edge-clamped stencil in minus_gradient_x!/minus_gradient_y! below). Override this, as done below for OGRectHydroGrid, only for grid backends whose fields carry real halo storage that needs to be kept in sync.\n\n\n\n\n\n","title":"FastHydrology.fill_halo!"},{"category":"function","location":"API_public/#FastHydrology.convolve!","page":"API Reference","text":"convolve!(grid::AbstractHydroGrid, dest, src, kernel)\n\n\nConvolve src with kernel and write the result into dest, both cell-centered fields on grid. kernel has the same number of dimensions as dest/src (i.e. 2D for a plain 2D field).\n\nUnlike the other grid-interface functions above, this one has a real default implementation rather than an error stub: it assumes dest/src already behave like plain arrays, which holds for most simple grid backends. This default calls ImageFiltering.imfilter! directly, which allocates fresh FFT plans and padded/complex buffers on every call (no caching hook available) – fine for a grid backend that only convolves occasionally, but both concrete grid types this package ships (ArrayHydroGrid, OGRectHydroGrid) override this instead with a conv_cache-backed call to cached_fft_convolve! (fftconvolution.jl), since `updatesmoothedpotentialgradients!calls this every solve of a coupled simulation. A new grid backend whose fields behave like plain arrays can either accept this default (if convolution is rare for it) or add its ownconv_cache` field and override, following the same pattern.\n\n\n\n\n\n","title":"FastHydrology.convolve!"},{"category":"function","location":"API_public/#FastHydrology.minus_gradient_x!","page":"API Reference","text":"minus_gradient_x!(grid::AbstractHydroGrid, dest, field)\n\n\nWrite -∂field/∂x into dest, both cell-centered fields on grid.\n\nThe default here assumes field/dest already behave like plain arrays and computes a central difference, clamping the neighbour index at the domain edges (i.e. edge-replicating) rather than reading from an explicit halo. This matches Oceananigans' own default zero-flux boundary condition at Bounded edges (verified empirically: with halo cells filled by fill_halo_regions!, OGRectHydroGrid's override below produces the same values as this formula would if given edge-replicated ghost cells). Override this, as done below for OGRectHydroGrid, for grid backends whose fields wrap a different underlying array storage.\n\n\n\n\n\n","title":"FastHydrology.minus_gradient_x!"},{"category":"function","location":"API_public/#FastHydrology.minus_gradient_y!","page":"API Reference","text":"minus_gradient_y!(grid::AbstractHydroGrid, dest, field)\n\n\nWrite -∂field/∂y into dest, both cell-centered fields on grid. See minus_gradient_x! for details on the default (plain-array) implementation.\n\n\n\n\n\n","title":"FastHydrology.minus_gradient_y!"},{"category":"function","location":"API_public/#FastHydrology.masked_mean","page":"API Reference","text":"masked_mean(grid::AbstractHydroGrid, field, mask) -> Any\n\n\nReturn the mean of field restricted to cells where mask == 1. Fuses the mask check into the reduction (branchless, via ifelse, so it vectorizes) instead of materializing a list of grounded indices first – benchmarked faster than the earlier findall(==(1), mask) + index-list approach at every grounded-fraction tested (1%-95%), including when amortized over the several masked* calls per Picard/coupling iteration in `resolveq!(water_flux.jl):findall's vector-growth cost and the scattered (non-sequential) memory access of indexing through aCartesianIndexlist both lose to a single dense, branchless pass overfield/mask` together, even though the dense pass touches every cell rather than just the grounded ones.\n\nAs with convolve!, the default here assumes field/mask already behave like plain arrays; override it, as done below for OGRectHydroGrid, for grid backends whose fields wrap a different underlying array storage.\n\n\n\n\n\n","title":"FastHydrology.masked_mean"},{"category":"function","location":"API_public/#FastHydrology.masked_max_abs","page":"API Reference","text":"masked_max_abs(grid::AbstractHydroGrid, field, mask) -> Any\n\n\nReturn the maximum of abs(field[i]) over cells where mask == 1. Paired with masked_max_abs_diff to build a relative convergence tolerance. Same fused, branchless reduction as masked_mean above – see its docstring for why this beats precomputing a grounded-index list.\n\n\n\n\n\n","title":"FastHydrology.masked_max_abs"},{"category":"function","location":"API_public/#FastHydrology.masked_max_abs_diff","page":"API Reference","text":"masked_max_abs_diff(\n    grid::AbstractHydroGrid,\n    a,\n    b,\n    mask\n) -> Any\n\n\nReturn the maximum of abs(a[i] - b[i]) over cells where mask == 1. Used to check convergence of fixed-point iterations (e.g. the dissipation-melt Picard loop in update_q!). Same fused, branchless reduction as masked_mean above – see its docstring for why this beats precomputing a grounded-index list.\n\n\n\n\n\n","title":"FastHydrology.masked_max_abs_diff"},{"category":"function","location":"API_public/#FastHydrology.overwrite_where!","page":"API Reference","text":"overwrite_where!(\n    grid::AbstractHydroGrid,\n    dest,\n    cond,\n    predicate,\n    src::Number;\n    scale\n)\n\n\nOverwrite cells of dest for which predicate(cond) holds with scale * src, where dest and cond are cell-centered fields on grid, predicate is a one-argument function (e.g. ==(0.0)), and src is a scalar. Fused into a single branchless (ifelse) pass rather than building the intermediate boolean mask array predicate.(cond) and assigning through it – same reasoning as masked_mean above: this runs inside update_S_inf!/update_N_inf! (effectivepressure.jl), which `updateN!calls up tomodel.maxcouplingiters` times per solve for N-dependent sliding laws, so avoiding a fresh full-size allocation every call is real, measured cost there.\n\nAs with convolve! and masked_mean, the default here assumes fields already behave like plain arrays; override it, as done below for OGRectHydroGrid, for grid backends whose fields wrap a different underlying array storage.\n\n\n\n\n\noverwrite_where!(\n    grid::AbstractHydroGrid,\n    dest,\n    cond,\n    predicate,\n    src;\n    scale\n)\n\n\nOverwrite cells of dest for which predicate(cond) holds with scale .* src, where dest, cond, and src are all cell-centered fields on grid. See the scalar-src method above for why this is a fused branchless pass rather than a boolean-mask assignment.\n\n\n\n\n\n","title":"FastHydrology.overwrite_where!"},{"category":"type","location":"API_public/#FastHydrology.AbstractHydroModel","page":"API Reference","text":"An abstract type for the hydrology model to be simulated. The model can hold revelant constants and model-specific fields.\n\n\n\n\n\n","title":"FastHydrology.AbstractHydroModel"},{"category":"type","location":"API_public/#FastHydrology.KazmierczakHydroModel","page":"API Reference","text":"The hydrology model described in Kazmierczak et al 2024 (https://doi.org/10.5194/tc-18-5887-2024). dx != dy grids are supported: every step of the water-flux calculation (including the stress-gradient coupling kernel in update_smoothed_potential_gradients!) works from dx and dy separately rather than assuming square cells.\n\nComposed of a KazmierczakParams (physical constants and solver configuration, immutable) and a KazmierczakWorkspace (every array buffer the solve touches, allocated once and updated in place), rather than one struct with the ~40 fields of both flattened together. This keeps the constructor's positional argument list short and grouped by struct instead of one long tuple that silently breaks if a field is inserted out of order.\n\nmodel.<field> (e.g. model.rho_w, model.q) still resolves exactly as before the split: Base.getproperty is overridden below to look up unrecognized field names on params then workspace, so nothing in waterflux.jl/effectivepressure.jl/sliding_law.jl/run.jl needed to change. Use model.params/model.workspace to get the sub-structs themselves.\n\n\n\n\n\n","title":"FastHydrology.KazmierczakHydroModel"},{"category":"type","location":"API_public/#FastHydrology.HABHydroModel","page":"API Reference","text":"The height above buoyancy (HAB) hydrology model described in Sec. 2.1.1 of Kazmierczak et al 2022 (https://doi.org/10.5194/tc-16-4537-2022).\n\n\n\n\n\n","title":"FastHydrology.HABHydroModel"},{"category":"type","location":"API_public/#FastHydrology.ShaktiHydroModel","page":"API Reference","text":"Wraps a Shakti.Simulation (see the separate Shakti.jl package) so it can be run as an AbstractHydroModel's time evolution via TimeSimulation. Shakti's Simulation already owns its own grid, state, and model parameters, so this wrapper holds nothing else – FastHydrology's run!(::TimeSimulation) for this model just delegates straight to Shakti.run!.\n\nDefined unconditionally (no dependency on Shakti itself, since the field is generic over S), but only usable once Shakti is loaded alongside FastHydrology: the run! method that dispatches on this type is added by the FastHydrologyShaktiExt package extension (see ext/FastHydrologyShaktiExt.jl), which Julia loads automatically once both packages are in scope (using FastHydrology, Shakti).\n\nArguments\n\nsim: a Shakti.Simulation built the usual way (Shakti.Simulation(grid, state, tsteps, dt, p, ...)).\n\n\n\n\n\n","title":"FastHydrology.ShaktiHydroModel"},{"category":"type","location":"API_public/#FastHydrology.AbstractSlidingLaw","page":"API Reference","text":"Basal sliding law used to compute the frictional-heating term taub * vb in the melt rate (Eq. 3, Sec. 2.2.1 of Kazmierczak et al 2024): mdot = (G + taub*vb - qT) / Lw + mdotw. `calctaub(a plain scalar formula) andupdatetaub!(the field-broadcast version actually used inresolveq!; see sliding_law.jl for why they're separate) both dispatch on this type to turnmodel.absvband the current effective pressurestate.N` into a basal shear stress tau_b [Pa].\n\nSplit into two branches:\n\nNoSlidingLaw/WeertmanSlidingLaw do not depend on N, so they contribute a source term that is either zero or a fixed offset computed once – no new fixed point to resolve.\nAbstractPressureDependentSlidingLaw (PowerPlasticSlidingLaw, RegularizedCoulombSlidingLaw) scale with N, so taub now depends on N which itself depends on q which depends on mdot which depends on taub: a genuine (q, N) fixed point. resolve_q! dispatches on this hierarchy to decide whether the existing q-only Picard loop needs widening to also update N each sweep (see water_flux.jl for the loop and the reasoning).\n\nThe two N-dependent laws mirror the two families of sliding law implemented in Yelmo.jl (Yelmo.jl/src/dyn/basal_dragging.jl, beta_method 1/2/4 and 3/5 respectively) so that, when this model is eventually coupled to Yelmo via Kryonomos.jl, both sides can be configured with numerically matching laws rather than independently-drifting formulas: PowerPlasticSlidingLaw matches _calc_beta_aa_power_plastic! (Bueler & van Pelt 2015) and RegularizedCoulombSlidingLaw matches _calc_beta_aa_reg_coulomb! (Joughin et al. 2019, GRL Eq. 2).\n\n\n\n\n\n","title":"FastHydrology.AbstractSlidingLaw"},{"category":"type","location":"API_public/#FastHydrology.NoSlidingLaw","page":"API Reference","text":"No frictional-heating feedback: taub = 0 everywhere, so mdot is unaffected by sliding (KazmierczakHydroModel's default `slidinglaw). Preserves the model's original behaviour, wheremdot_in` alone is assumed to already represent the full melt rate.\n\n\n\n\n\n","title":"FastHydrology.NoSlidingLaw"},{"category":"type","location":"API_public/#FastHydrology.WeertmanSlidingLaw","page":"API Reference","text":"Weertman-type power sliding law: taub = C * |vb|^q, independent of effective pressure N. Included for comparison/testing and for domains where N-independent sliding is the intended approximation; since it does not depend on N it does not introduce a (q, N) feedback and costs nothing beyond a single elementwise evaluation.\n\nFields\n\nC::T: sliding coefficient [Pa (s/m)^q]\nq::T: velocity exponent (dimensionless; classically 1/n with n Glen's law exponent, so ~1/3)\n\n\n\n\n\n","title":"FastHydrology.WeertmanSlidingLaw"},{"category":"type","location":"API_public/#FastHydrology.PowerPlasticSlidingLaw","page":"API Reference","text":"Power-plastic sliding law (Bueler & van Pelt 2015): taub = ctill * N * (|vb| / u0)^q. Matches Yelmo.jl's `calcbetaaapowerplastic!(beta_method1 whenq = 1, 2 for generalq`).\n\nFields\n\nc_till::T: till-strength coefficient (dimensionless, ~ tan of the till friction angle)\nq::T: velocity exponent (dimensionless; q = 1 gives a linear-in-velocity law)\nu0::T: velocity scale [m/s]\n\n\n\n\n\n","title":"FastHydrology.PowerPlasticSlidingLaw"},{"category":"type","location":"API_public/#FastHydrology.RegularizedCoulombSlidingLaw","page":"API Reference","text":"Regularized-Coulomb sliding law (Joughin et al. 2019, GRL Eq. 2): taub = ctill * N * (|vb| / (|vb| + u0))^q. Saturates toward the Coulomb-friction limit ctill * N as |vb| -> infinity; behaves like a Weertman power law for |vb| << u0. Matches Yelmo.jl's `calcbetaaaregcoulomb!(beta_method` 3/5).\n\nFields\n\nc_till::T: till-strength coefficient (dimensionless, ~ tan of the till friction angle)\nq::T: velocity exponent (dimensionless)\nu0::T: velocity scale [m/s]\n\n\n\n\n\n","title":"FastHydrology.RegularizedCoulombSlidingLaw"},{"category":"function","location":"API_public/#FastHydrology.calc_tau_b","page":"API Reference","text":"calc_tau_b(_::NoSlidingLaw, N, abs_v_b) -> Any\n\n\nBasal shear stress taub [Pa] from law, given the current effective pressure N [Pa] and the magnitude of the basal sliding velocity `absvb` [m/s]. Feeds the frictional-heating term taub*vb/Lw of the melt rate (Eq. 3, Sec. 2.2.1 of Kazmierczak et al 2024). See the AbstractSlidingLaw docstring in model.jl for the physics/provenance of each law. A plain scalar function (used for tests/diagnostics on ordinary numbers) – update_tau_b! below is the version actually used inside the model's field broadcasts.\n\n\n\n\n\n","title":"FastHydrology.calc_tau_b"},{"category":"function","location":"API_public/#FastHydrology.update_tau_b!","page":"API Reference","text":"update_tau_b!(model, state, _::NoSlidingLaw)\n\n\nUpdate model.tau_b in place from sliding_law and the current state.N/model.abs_v_b. Duplicates the calc_tau_b formulas above with the law's parameters pulled out as plain scalar locals first, rather than calling calc_tau_b from inside the @. broadcast, because OGRectHydroGrid's Field broadcasting goes through Oceananigans' AbstractOperations machinery, which only recognises registered arithmetic/operators inside a broadcast – not arbitrary multi-argument user functions taking a struct argument. Writing the formula with plain arithmetic (+, *, /, ^) on fields/arrays and scalar locals, exactly as the rest of this model already does (e.g. update_N_inf! in effective_pressure.jl), works uniformly for both grid backends.\n\n\n\n\n\n","title":"FastHydrology.update_tau_b!"},{"category":"type","location":"API_public/#FastHydrology.AbstractHydroState","page":"API Reference","text":"Abstract type for a hydrology state to hold fields common to all hydrology models.\n\n\n\n\n\n","title":"FastHydrology.AbstractHydroState"},{"category":"type","location":"API_public/#FastHydrology.HydroState","page":"API Reference","text":"The struct for the HydroState which includes physical fields important to hydrology.\n\n\n\n\n\n","title":"FastHydrology.HydroState"},{"category":"type","location":"API_public/#FastHydrology.AbstractSimulation","page":"API Reference","text":"Abstract type for a simulation struct. The simulation should hold important information such as model fields and constants, the grid, and a state with physically relevant fields.  The method that will perform the computation for the simulation should be implemented for each subtype of AbstractSimulation.\n\n\n\n\n\n","title":"FastHydrology.AbstractSimulation"},{"category":"type","location":"API_public/#FastHydrology.TimeSimulation","page":"API Reference","text":"A struct for time evolution simulations. Wraps whichever AbstractHydroModel owns the actual time evolution logic (e.g. ShaktiHydroModel); run!(sim::TimeSimulation) dispatches on M to find the model-specific method.\n\n\n\n\n\n","title":"FastHydrology.TimeSimulation"},{"category":"type","location":"API_public/#FastHydrology.SteadyStateSimulation","page":"API Reference","text":"A struct for steady-state simulations. For further details see the corresponding constructor.\n\n\n\n\n\n","title":"FastHydrology.SteadyStateSimulation"},{"category":"function","location":"API_public/#FastHydrology.run!","page":"API Reference","text":"run!(sim::SteadyStateSimulation)\n\n\nA function to run a steady-state simulation.\n\n\n\n\n\n","title":"FastHydrology.run!"},{"category":"function","location":"API_public/#FastHydrology.step!","page":"API Reference","text":"Advances a TimeSimulation by a single timestep, dispatching on its model type – e.g. for ShaktiHydroModel, the FastHydrologyShaktiExt package extension adds a method that calls Shakti.step! directly (see ext/FastHydrologyShaktiExt.jl).\n\nExists as a stub here (no methods) so the name is owned by FastHydrology and can be extended by package extensions, and so a coupled driver (e.g. an ice flow model updating geometry every timestep) can call step! in its own loop instead of using run!, which owns the whole time loop itself.\n\n\n\n\n\n","title":"FastHydrology.step!"},{"category":"function","location":"API_public/#FastHydrology.update_steady_state!","page":"API Reference","text":"update_steady_state!(\n    model::KazmierczakHydroModel,\n    grid::AbstractHydroGrid,\n    state::HydroState\n)\n\n\nUpdate the model and state variables for the Kazmierczak et al 2024 model steady-state calculation. We specifically update first the distributed water flux q, then the effective pressure N, and then the water layer thickness W – W must come after N because its ArealConduitThickness closure (see AbstractWaterThicknessAlgorithm in model.jl) reads model.S_inf, which update_N! (via its own update_S_inf! call) is what keeps current. state.W itself feeds back into nothing else in the model, so computing it last is safe.\n\nWhen model.sliding_law is N-dependent (see waterflux.jl's `resolveq!),updateq!already converges q and N together internally, so the explicitupdateN!/update_W!` calls below just recompute the same converged values from the same final q – redundant but harmless (both are pure functions of q and the unchanged geometry/state), kept for a uniform call sequence across all sliding-law choices.\n\n\n\n\n\nupdate_steady_state!(\n    model::HABHydroModel,\n    grid::AbstractHydroGrid,\n    state::HydroState\n)\n\n\nUpdate the state variable of effective pressure according to Eq. (3) of the paper Kazmierczak et al 2022 (https://doi.org/10.5194/tc-16-4537-2022). The model essentially assumes that ocean water infiltrates the ice sheet from the grounding line upwards to grounded ice regions where the bedrock is below sea level.\n\n\n\n\n\n","title":"FastHydrology.update_steady_state!"},{"category":"type","location":"API_public/#FastHydrology.AbstractOutputWriter","page":"API Reference","text":"How periodic simulation output is recorded to disk – multiple dispatch on the concrete subtype picks the file format. Currently only NetCDFOutputWriter is provided, since NCDatasets is already a hard dependency of FastHydrology and every existing coupled-driver script already writes NetCDF output by hand (see e.g. Kryonomos.jl's MISMIP scripts) – this factors that pattern into a reusable, resumable writer instead of it being hand-rolled anew in every driver. See write_output!/close_output! below, and save_checkpoint/ load_checkpoint! (checkpoint.jl) for the related but separate full-state resume mechanism.\n\nNotes\n\nFastHydrology's own models (KazmierczakHydroModel/HABHydroModel) are steady-state (see their docstrings): they have no internal time loop of their own for a writer to hook into automatically – SteadyStateSimulation re-solves fresh from the current geometry on every run! call, and owns no loop across calls. An AbstractOutputWriter is instead meant to be driven directly from whichever loop owns the repeated run! calls – typically a coupled driver's own timestep loop (e.g. an ice-flow model), which already decides when to call run!; call write_output! there too, at whatever interval that loop chooses.\n\nTimeSimulation currently only wraps ShaktiHydroModel, whose run! already delegates entirely to Shakti.run! – which has its own, independent periodic-output/checkpoint machinery (checkpoint_every/checkpoint_path/restart_path, forwarded straight through as kwargs already) that already solves this same problem for that model. There is nothing for AbstractOutputWriter to hook into there without duplicating what Shakti already does; it exists for the models (and coupled-driver patterns) that have no such mechanism of their own.\n\n\n\n\n\n","title":"FastHydrology.AbstractOutputWriter"},{"category":"type","location":"API_public/#FastHydrology.NetCDFOutputWriter","page":"API Reference","text":"Records named fields to a NetCDF file, one time-slice at a time, along an unlimited time dimension – so a killed run leaves every slice written before the kill intact and readable (NetCDF's own on-disk file structure, not a buffer FastHydrology holds in memory), instead of losing everything the way a single write-at-the-end script does.\n\nBuild with the NetCDFOutputWriter constructor; write a time-slice with write_output!; close with close_output! when done.\n\n\n\n\n\n","title":"FastHydrology.NetCDFOutputWriter"},{"category":"function","location":"API_public/#FastHydrology.write_output!","page":"API Reference","text":"write_output!(\n    writer::NetCDFOutputWriter,\n    step::Integer,\n    time::Real,\n    fields::NamedTuple\n)\n\n\nAppends one time-slice to writer's file: fields[name] for each name in writer.field_names (a NamedTuple, e.g. (N = state.N, W = state.W) – values can be either an Oceananigans Field or a plain Array, matching whichever grid backend is in use), tagged with step/time. Flushes to disk immediately (NCDatasets.sync) so the write actually lands even if the process is killed right after this call returns – the entire point of this writer.\n\n\n\n\n\n","title":"FastHydrology.write_output!"},{"category":"function","location":"API_public/#FastHydrology.close_output!","page":"API Reference","text":"close_output!(writer::NetCDFOutputWriter)\n\n\nCloses writer's underlying NetCDF file handle.\n\n\n\n\n\n","title":"FastHydrology.close_output!"},{"category":"function","location":"API_public/#FastHydrology.save_checkpoint","page":"API Reference","text":"save_checkpoint(\n    path::String,\n    state::AbstractHydroState,\n    step::Integer,\n    time::Real\n)\n\n\nSaves every field of state (generic over any AbstractHydroState subtype via fieldnames, so this works for HydroState's mask/h/b/N/W without hardcoding them) to path, tagged with step/time.\n\nWritten to a temp file and renamed into place (same filesystem, so mv is an atomic rename) rather than written directly to path, so a crash mid checkpoint-write can't leave a truncated/corrupt checkpoint behind – the previous good checkpoint at path stays intact until the new one has fully landed. Mirrors Shakti.jl's own save_checkpoint (Shakti/src/checkpoint.jl), using NetCDF (via NCDatasets, already a hard dependency here) instead of JLD2.\n\nNotes\n\nFastHydrology's own models (KazmierczakHydroModel/HABHydroModel) are steady-state (see their docstrings): state.N/state.W are fully recomputed from state.mask/ h/b and the model's own input fields on every run! call, so there is no hydrology-side integration state that genuinely needs resuming. This checkpoint exists for the coupled-driver pattern instead (e.g. an ice-flow model calling run! once per iteration of its own loop – see Kryonomos.jl's MISMIP scripts): resuming a killed job needs the ice model's own state (geometry, velocity, ...) restored too, which is outside FastHydrology's scope – this only restores the hydrology side (the exact mask/h/b inputs the checkpointed step used, plus the N/W outputs, in case a caller wants them), so the driver's own loop can pick back up at step/time knowing exactly which iteration was last saved.\n\n\n\n\n\n","title":"FastHydrology.save_checkpoint"},{"category":"function","location":"API_public/#FastHydrology.load_checkpoint!","page":"API Reference","text":"load_checkpoint!(\n    state::AbstractHydroState,\n    path::String\n) -> Tuple{Any, Any}\n\n\nRestores state's fields in place from a checkpoint written by save_checkpoint, and returns (step, time) so the caller's own loop knows where to resume. Generic over any AbstractHydroState subtype via fieldnames, matching save_checkpoint.\n\n\n\n\n\n","title":"FastHydrology.load_checkpoint!"},{"category":"function","location":"API_public/#FastHydrology.update_q!","page":"API Reference","text":"update_q!(\n    model::KazmierczakHydroModel,\n    grid::AbstractHydroGrid,\n    state::HydroState\n)\n\n\nFor the Kazmierczak et al 2024 model, update the distributed water flux q [m2 s-1] via a recursive algorithm following Le Brocq's code for Le Brocq et al 2006 (https://doi.org/10.1016/j.cageo.2006.05.003). First we update the geometric potential that depends on the icethickness h and bedrock elevation b. Then we fill up the local minima of this potential to avoid the issue of water getting stuck. We then update the gradients of the potential in x, y. Further, we smooth these gradients with a convolution in order to incorporate the effects of the stress-gradient coupling. For the latter concept, see for example Eq. (8.98) from Cuffey & Patterson 2010 book, Eq. (15) from Kamb et al 1986, Gudmundsson 2002, and references therein. Finally, we calculate the scalar water flux out of each grid cell psiout following the aforementioned recursive algorithm from Le Brocq. To go from psiout to q, we use a correction factor, here called corfac, that can be derived using the definition of psi_out given by Eq. (R4) of the referee reports of Kazmierczak et al 2024 (https://egusphere.copernicus.org/preprints/2024/egusphere-2024-466/egusphere-2024-466-AC1-supplement.pdf). Finally, q is clamped to 0 <= q <= perYear2perSecond(1e5) – KORI-ULB's own SubWaterFlux.m (https://github.com/FrankPat/Kori-ULB/blob/main/subroutines/SubWaterFlux.m) applies this same 1e5 m2/yr limit set by Frank Pattyn for numerical stability, but to its per-year-native flw; q here is SI (m2/s), so the limit needs the same per-year -> per-second conversion as the other data-loader/unit fixes in this codebase, not the bare literal 1e5.\n\nThe water source that feeds the routing algorithm is not just the externally-supplied basal melt mdot, but also, if enabled, the dissipation melt rate ṁw = |q * grad(phi0)| / Lw generated by the flux itself as it moves down the hydraulic gradient, and the frictional-heating term taub*vb/Lw from `model.slidinglaw(Eq. (3), Sec. 2.2.1/2.2.2 of Kazmierczak et al 2024). The paper drops the dissipation term as negligible; we keep it as an opt-out (model.dissipationmelt, set via thedissipationmeltkeyword of the constructor) rather than always computing it, since with it off (and no sliding law)psiout/qreduce to a single pass with no feedback to resolve at all. When on, we use the unsmoothed gradient of phi0, not the smoothed one used for flow routing below -- the smoothing is a numerical device for stress-gradient coupling, not part of the actual local driving gradient that does work on the water, and this matches how the analogous conduit dissipation term (model.Q * model.absgradphi0) is already written inupdateN_inf!`.\n\nThe dissipation term depends on q, which is itself the output of the routing algorithm, so we Picard-iterate: recompute the source from the current q, re-run the routing algorithm, and stop once q stops changing (relative to its own peak magnitude) to within model.dissipation_rtol, capped at model.max_dissipation_iters sweeps as a safety net. The frictional-heating term depends on the effective pressure N instead, which is itself downstream of q (via update_N!, called after update_q! in update_steady_state!) – so an N-dependent model.sliding_law (PowerPlasticSlidingLaw, RegularizedCoulombSlidingLaw) turns this into a second, coupled fixed point on (q, N). Rather than nesting a second Picard loop around the first (which would fully reconverge q for a stale N every outer sweep), we widen the existing loop: each sweep recomputes taub from the current N, routes q, and then also updates W and N in place before the next sweep, converging jointly. `model.slidinglaw's type determines whichresolveq!method runs:NoSlidingLaw/WeertmanSlidingLawdo not depend on N (the latter contributes a fixed source term, computed but not iterated on), so they fall back to the original q-only dispatch onmodel.dissipationmelt;AbstractPressureDependentSlidingLawalways takes the joint (q, N) loop, usingmodel.maxcouplingiters/model.couplingrtolregardless ofmodel.dissipationmelt(which only decides whether the dissipation term is added inside that loop, viaadddissipationterm!`).\n\n\n\n\n\n","title":"FastHydrology.update_q!"},{"category":"function","location":"API_public/#FastHydrology.update_phi0!","page":"API Reference","text":"update_phi0!(\n    model::KazmierczakHydroModel,\n    grid::AbstractHydroGrid,\n    state::HydroState\n)\n\n\nUpdate the geometric potential phi0 and fill halo points.\n\n\n\n\n\n","title":"FastHydrology.update_phi0!"},{"category":"function","location":"API_public/#FastHydrology.potential_filling!","page":"API Reference","text":"potential_filling!(\n    model::KazmierczakHydroModel,\n    grid::AbstractHydroGrid,\n    state::HydroState\n)\n\n\nUpdates phi0 and consequently also updates h to reflect changes in phi0. It fills the local minima of phi0 to avoid water getting stuck in there.\n\n\n\n\n\n","title":"FastHydrology.potential_filling!"},{"category":"function","location":"API_public/#FastHydrology.update_potential_gradients!","page":"API Reference","text":"update_potential_gradients!(\n    model::KazmierczakHydroModel,\n    grid::AbstractHydroGrid\n)\n\n\nCompute (the negative of) the gradients of the geometric potential phi0 and its absolute value.\n\n\n\n\n\n","title":"FastHydrology.update_potential_gradients!"},{"category":"function","location":"API_public/#FastHydrology.update_smoothed_potential_gradients!","page":"API Reference","text":"update_smoothed_potential_gradients!(\n    model::KazmierczakHydroModel,\n    grid::AbstractHydroGrid,\n    state::HydroState\n)\n\n\nUpdate the smoothed geometric potentials to incorporate the effects of the stress-gradient coupling. See also the description of the update_q! function.\n\nThe water flux at a given point is influenced by variations in ice thickness some distance away. To account for this we perform a convolution of the gradient of the potential such that the influence of nearby points is now incorporated into the value of the gradient of the potential at that point.\n\n\n\n\n\n","title":"FastHydrology.update_smoothed_potential_gradients!"},{"category":"function","location":"API_public/#FastHydrology.accumulate_psi_out!","page":"API Reference","text":"accumulate_psi_out!(\n    model::KazmierczakHydroModel,\n    i,\n    j,\n    grid::AbstractHydroGrid,\n    state::HydroState,\n    call_count::Base.RefValue{Int64}\n) -> Any\n\n\nHelper function to the recursive function to calculate the psi_out for every grid cell that has grounded ice.\n\ncall_count is a per-sweep counter, shared by reference across the whole recursion tree started by update_psi_out!, that caps the total number of cells this recursion is allowed to visit at model.max_psi_out_calls – mirroring KORI-ULB's own funcnt <= 5e4 safety cap in DpareaWarGds.m (https://github.com/FrankPat/Kori-ULB/blob/main/subroutines/DpareaWarGds.m). Without it, a large or unusually convoluted flow-routing domain could recurse deep enough to hit Julia's call stack limit and crash with a StackOverflowError instead of failing predictably. Once the cap is hit, the current cell is treated as a terminal source (its own mdot_total contribution only, no further upstream accumulation) so the sweep still terminates with a (locally truncated but finite) result rather than crashing.\n\n\n\n\n\n","title":"FastHydrology.accumulate_psi_out!"},{"category":"function","location":"API_public/#FastHydrology.update_psi_out!","page":"API Reference","text":"update_psi_out!(\n    model::KazmierczakHydroModel,\n    grid::AbstractHydroGrid,\n    state::HydroState\n)\n\n\nRecursive function to calculate the psiout for every grid cell that has grounded ice. We initialize psiout to -1 since it is by definition positive semi-definite and hence we know that if a grid point has negative psi_out, it is still unvisited.\n\n\n\n\n\n","title":"FastHydrology.update_psi_out!"},{"category":"function","location":"API_public/#FastHydrology.update_psi_out_iterative!","page":"API Reference","text":"update_psi_out_iterative!(\n    model::KazmierczakHydroModel,\n    grid::AbstractHydroGrid,\n    state::HydroState\n)\n\n\nIterative counterpart to update_psi_out!/accumulate_psi_out!: computes the exact same psiout field with an explicit stack standing in for the Julia call stack, instead of true recursion. It exists purely to sidestep the recursive version's call-stack usage (and per-frame dispatch/allocation overhead) on large domains, not to change the algorithm – so it must reproduce the recursive version's output cell-for-cell, `maxpsioutcalls` cutoff included.\n\nEach stack entry is (i, j, k): k == 0 means cell (i, j) has not been visited yet (do the first-visit work below); 1 <= k <= 4 means its first k - 1 neighbours (in the same dirs order accumulate_psi_out! iterates) have already had their contribution folded into psi_out[i, j], and neighbour k is next; k == 5 means all four neighbours are done and psi_out[i, j] is ready to be clamped and finalized. Critically, when a not-yet-visited neighbour is found, we push it without advancing the current frame's k – so once that child is popped (fully resolved, visited = 1), we revisit the same (i, j, k) frame, and it takes the \"already visited\" branch to fold the now-ready child value in and move on to k + 1. This is exactly how a return value flows back to a paused caller in real recursion, just made explicit.\n\n\n\n\n\n","title":"FastHydrology.update_psi_out_iterative!"},{"category":"function","location":"API_public/#FastHydrology.route_psi_out!","page":"API Reference","text":"route_psi_out!(\n    model::KazmierczakHydroModel,\n    grid::AbstractHydroGrid,\n    state::HydroState\n)\n\n\nComputes psiout for the current sweep, dispatching on `model.psioutalgorithm(RecursivePsiOut(),IterativePsiOut(), orTopologicalPsiOut()-- seeAbstractPsiOutAlgorithm's docstring in model.jl) to the recursiveupdatepsiout!, the stack-basedupdatepsioutiterative!, or the sorted single-passupdatepsiouttopological!. Everyresolveq!method calls this instead ofupdatepsiout!` directly, so the algorithm choice applies uniformly regardless of which sliding law/dissipation-melt combination is active.\n\n\n\n\n\n","title":"FastHydrology.route_psi_out!"},{"category":"type","location":"API_public/#FastHydrology.AbstractPsiOutAlgorithm","page":"API Reference","text":"Trait selecting which flow-routing implementation resolve_q! uses to compute psiout each sweep (see KazmierczakHydroModel's `psioutalgorithmkeyword). Stored as a type parameter, resolved by multiple dispatch at compile time viaroutepsiout!` in waterflux.jl, the same pattern as AbstractDissipationMelt above.\n\nRecursivePsiOut (update_psi_out!/accumulate_psi_out!) and IterativePsiOut (update_psi_out_iterative!) compute exactly the same field – verified to match bit-for-bit, max_psi_out_calls cutoff included, on synthetic grids and on real Thwaites/pan-Antarctica datasets – so switching between them changes performance and robustness, not results:\n\nRecursivePsiOut (the default, preserving prior behaviour) is substantially faster (~30-40x faster per sweep, benchmarked on Thwaites 2km and pan-Antarctica 8km) since Julia's native call stack has far less overhead than an explicit heap-allocated stack. But it recurses as deep as the longest flow-routing chain in the domain, which for real (non-toy) ice-sheet grids can exceed the calling process's ulimit -s and crash with a StackOverflowError – independent of, and potentially before, max_psi_out_calls ever binds (that cap limits total cells visited per sweep, not recursion depth along any one chain).\nIterativePsiOut has no such requirement (its \"stack\" is a plain Vector, bounded only by heap memory), at the cost of that ~30-40x slowdown. Both RecursivePsiOut and IterativePsiOut are, at bottom, the same graph traversal (a DFS with an explicit or implicit stack, revisiting cells to fold child contributions back in) – neither exploits the fact that the underlying dependency graph has no cycles.\n\nTopologicalPsiOut is a genuinely different algorithm, not just a different traversal strategy for the same one: since psi_out[i,j] only ever depends on hydraulically-upstream neighbours (w > 0), if the dependency graph those w > 0 edges define is a DAG, it can be processed in one O(N) pass via Kahn's algorithm (a BFS ordered by in-degree: a cell becomes ready once every upstream neighbour that flows into it has already been finalized, at which point its own contribution is added to whichever neighbours it flows into and their remaining in-degree is decremented, in turn making more cells ready) – no recursion, no explicit revisit stack, and no need for max_psi_out_calls.\n\nThe load-bearing word above is \"if\". w is evaluated from minus_grad_phi0_sx/sy – the smoothed gradient (from update_smoothed_potential_gradients!'s stress-gradient-coupling convolution) when longcoupwater != 0, or the raw central-difference gradient (from update_potential_gradients!) when longcoupwater == 0 – and neither is guaranteed to be a true conservative/gradient-derived vector field once normalized per cell, so the graph they define is not guaranteed acyclic. It is acyclic on every idealized synthetic test grid used in this package's own test suite (smooth, monotonic sloped surfaces), at any longcoupwater. It is not acyclic on the real Thwaites-2km dataset used to develop this function, confirmed by explicit DFS cycle detection (not inferred) – and, contrary to an earlier version of this docstring's claim, this is true regardless of longcoupwater: cycles affect roughly half of all grounded cells at the model's default longcoupwater = 5.0, but more than half (37514 of 51945) with smoothing turned off entirely (longcoupwater = 0), and persist at a coarsened ~8km version of the same grid too (2573 of 3242). Real, noisy topography is apparently enough on its own to produce local circulation in a per-cell-normalized central-difference gradient field – this is not specific to the stress-gradient-coupling smoothing step. There is currently no known setting that makes this algorithm reliably exact on real (non-idealized) ice-sheet data.\n\nRecursion-based traversal doesn't actually solve the underlying problem either – RecursivePsiOut/IterativePsiOut silently return an early, incomplete snapshot for a cell that's already mid-computation when a cycle loops back to it, giving some order-dependent approximate value with no warning at all – but it never leaves a cell entirely unprocessed the way a topological sort must, so it doesn't fail visibly. Given that, this implementation treats a detected cycle as a hard error by default (TopologicalPsiOut(), allow_cycles = false): silently returning a badly wrong field (in the case that surfaced this, q correlation against RecursivePsiOut collapsed from -0.03 to ~0.53 depending on configuration, neither acceptable) is worse than failing loudly. Pass allow_cycles = true to instead get the old \"locally truncated but finite\" behaviour – cells inside a cycle keep only their partial upstream contributions from outside it (their own source term never added) – with a warning (once per sweep) instead of an error. In practice, expect this algorithm to error on any real (non-idealized) domain; it is exact and useful on synthetic/idealized grids, or on a real domain a caller has independently verified to be acyclic for their specific gradient field. Prefer RecursivePsiOut/IterativePsiOut for real ice-sheet data.\n\n\n\n\n\n","title":"FastHydrology.AbstractPsiOutAlgorithm"},{"category":"type","location":"API_public/#FastHydrology.RecursivePsiOut","page":"API Reference","text":"Route psiout with the original recursive algorithm (`updatepsiout!/accumulatepsiout!;KazmierczakHydroModel's defaultpsiout_algorithm). Substantially faster than [IterativePsiOut](@ref) (~30-40x per sweep, benchmarked on Thwaites 2km and pan-Antarctica 8km), but recurses as deep as the domain's longest flow-routing chain -- see [AbstractPsiOutAlgorithm`](@ref)'s docstring for when that's a problem.\n\n\n\n\n\n","title":"FastHydrology.RecursivePsiOut"},{"category":"type","location":"API_public/#FastHydrology.IterativePsiOut","page":"API Reference","text":"Route psiout with a stack-based rewrite of the same algorithm (`updatepsioutiterative!), verified to match [RecursivePsiOut](@ref) bit-for-bit. Its \"stack\" is a heap-allocatedVectorrather than the native call stack, so it has no recursion-depth limit -- at the cost of that ~30-40x slowdown. See [AbstractPsiOutAlgorithm`](@ref)'s docstring for the full trade-off.\n\n\n\n\n\n","title":"FastHydrology.IterativePsiOut"},{"category":"type","location":"API_public/#FastHydrology.TopologicalPsiOut","page":"API Reference","text":"Route psiout with a single-pass topological sort (Kahn's algorithm) over the actual flow-direction dependency graph (`updatepsiouttopological!), rather than traversing it via recursion/an explicit revisit stack. Exact (matchesRecursivePsiOut/IterativePsiOutto floating-point precision) only when that graph is genuinely acyclic -- true on every idealized synthetic test grid in this package's test suite, but confirmed **not** true (real cycles, at anylongcoupwater) on the real Thwaites-2km dataset used to develop this function. See [AbstractPsiOutAlgorithm](@ref)'s docstring for the full story, including whylongcoupwater = 0` does not make this reliably safe on real data either.\n\nFields\n\nallow_cycles::Bool: if false (the default), a detected cycle throws an error rather than silently returning a wrong field. If true, falls back to the \"locally truncated but finite\" behaviour instead (a warning, not an error) – cells inside a cycle keep only their partial upstream contributions.\n\n\n\n\n\n","title":"FastHydrology.TopologicalPsiOut"},{"category":"function","location":"API_public/#FastHydrology.update_W!","page":"API Reference","text":"update_W!(\n    model::KazmierczakHydroModel,\n    grid::AbstractHydroGrid,\n    state::HydroState\n)\n\n\nUpdate the water layer thickness state.W, dispatching on model.water_thickness_algorithm (ArealConduitThickness(), DarcyWeisbachThickness(), or LaminarThickness() – see AbstractWaterThicknessAlgorithm's docstring in model.jl) to whichever single closure is selected, so only that closure's fields are touched each call.\n\nMust run after update_N! (specifically after its update_S_inf! call), not before – ArealConduitThickness reads model.S_inf, which update_N! is what keeps current. update_steady_state! in run.jl calls update_q!, then update_N!, then update_W! in that order for this reason; state.W itself feeds back into nothing else in the model, so it is safe to compute last.\n\n\n\n\n\nupdate_W!(\n    model::KazmierczakHydroModel,\n    grid::AbstractHydroGrid,\n    state::HydroState,\n    _::ArealConduitThickness\n)\n\n\nArealConduitThickness: report model.S_inf / model.l_c – the conduit cross-section smeared over the inter-conduit spacing – as state.W, unclamped. See AbstractWaterThicknessAlgorithm's docstring in model.jl for the derivation and why S_inf (not H) is the right numerator.\n\n\n\n\n\nupdate_W!(\n    model::KazmierczakHydroModel,\n    grid::AbstractHydroGrid,\n    state::HydroState,\n    algorithm::DarcyWeisbachThickness\n)\n\n\nDarcyWeisbachThickness (KazmierczakHydroModel's default water_thickness_algorithm): invert the turbulent parallel-plate Darcy-Weisbach closure for a wide slot, d = (f*rho_w*q^2 / (4*abs_grad_phi0))^(1/3). model.f here is the same Darcy-Weisbach friction factor K24's own conduit closure uses (folded into K by update_S_inf!) – one shared parameter, not a second one introduced for this closure, since both derive from the same underlying physics (Schoof 2010/Clarke 1996); see f's field comment on KazmierczakParams in model.jl. Uses a domain-wide masked mean of the potential gradient by default (gradient_convention = MeanGradient(), the same averaging convention LaminarThickness uses, kept consistent between the package's two sheet-flow closures – see AbstractGradientConvention's docstring in model.jl), or, with gradient_convention = LocalGradient(), the local, per-cell gradient instead (matching the convention update_S_inf! uses). The turbulent analogue of LaminarThickness below, and the closure consistent with K24's own turbulent-flow assumption for q (unlike LaminarThickness) – clamped to [Wmin, Wmax] since it represents the same kind of thin-sheet quantity. The + 1e-15 guards degenerate cells where abs_grad_phi0 is exactly zero (e.g. flat cells outside the glacier extent). Uses q*q rather than q^2.0: Float64^Float64 dispatches to libm's pow() per element, ~17x slower (benchmarked) than a plain multiply for no numerical difference, and q^2 (integer literal) hits a separate issue – @.'s literal_pow rewrite isn't supported by Oceananigans' AbstractOperation broadcasting.\n\n\n\n\n\nupdate_W!(\n    model::KazmierczakHydroModel,\n    grid::AbstractHydroGrid,\n    state::HydroState,\n    algorithm::LaminarThickness\n)\n\n\nLaminarThickness: the original closure (Eq. 8, Kazmierczak et al 2022 / Le Brocq et al 2009 Eq. 2), clamped to [Wmin, Wmax]. Kept for direct comparison against Kori-ULB's Wd/SHAKTI's laminar sheet closure – see AbstractWaterThicknessAlgorithm's docstring in model.jl for why this closure is not the default (DarcyWeisbachThickness is). Uses a single domain-mean smoothed gradient magnitude by default (gradient_convention = MeanGradient()), faithfully matching Kori-ULB's own mean(gdsmag(...)) in SubWaterFlux.m, and matching DarcyWeisbachThickness's own default too (kept consistent between the package's two sheet-flow closures); pass gradient_convention = LocalGradient() for the local-per-cell-gradient variant instead, e.g. to isolate how much of a closure's spatial pattern comes from that domain-mean averaging versus from its own physics – DarcyWeisbachThickness's gradient_convention runs the identical comparison for the turbulent closure.\n\n\n\n\n\n","title":"FastHydrology.update_W!"},{"category":"type","location":"API_public/#FastHydrology.AbstractWaterThicknessAlgorithm","page":"API Reference","text":"Trait selecting which closure update_W! uses to compute the reportable subglacial water thickness state.W (see KazmierczakHydroModel's water_thickness_algorithm keyword). Stored as a type parameter, resolved by multiple dispatch at compile time – the same pattern as AbstractPsiOutAlgorithm/AbstractDissipationMelt above – so only the selected closure's fields are touched each call, not all three.\n\nstate.W is documented as a water layer thickness – an areal, grid-cell-averaged quantity, the same kind of thing a sheet model's W is – so every closure offered here reports one. K24's own local conduit depth model.H (H = (1-kappa)*H_hard + kappa*H_soft, already computed by update_H! for N_inf) deliberately has no closure here: it answers a different question (the depth inside one conduit, not smeared over a grid cell) and is not areal, so reporting it as state.W would be comparing apples to oranges against the other closures. Use model.H directly if you want that quantity – there is nothing stopping you, it just isn't one of state.W's options.\n\nThe three closures still answer different physical questions and are not interchangeable:\n\nDarcyWeisbachThickness (the default): inverts the turbulent parallel-plate Darcy-Weisbach closure for a wide slot – the turbulent analogue of the laminar Le Brocq/Weertman closure below, and the closure consistent with K24's own turbulent-flow assumption for q – using the model's own distributed flux q and, by default, a domain-wide masked mean of the potential gradient (gradient_convention = MeanGradient(), the same averaging convention LaminarThickness uses, kept consistent between the package's two sheet-flow closures): d = (f*rho_w*q^2 / (4*abs_grad_phi0))^(1/3). Clamped to [Wmin, Wmax], since it represents a thin-sheet quantity. Pass gradient_convention = LocalGradient() for the local, per-cell gradient instead (matching the convention update_S_inf! uses) – see AbstractGradientConvention's docstring below.\nArealConduitThickness: S_inf / l_c, the conduit's cross-sectional area smeared over the inter-conduit spacing – a grid-cell-averaged \"equivalent film thickness\" comparable to a sheet model's W, derived from the same turbulent Manning-Strickler S_inf that drives N_inf. Bed-type independent (kappa only blends H's shape assumption, not S_inf itself) and not clamped to [Wmin, Wmax]: those bounds were chosen for a thin distributed sheet, and this areal quantity can legitimately exceed them too (a dense-enough conduit network smeared over its spacing is not bounded the same way a single laminar/turbulent sheet-flow inversion is).\nLaminarThickness: the original closure (Eq. 8, Kazmierczak et al 2022 / Le Brocq et al 2009 Eq. 2), d = (12*eta_w*q / abs_grad_phi0_s)^(1/3), using a single domain-mean smoothed gradient by default (matching Kori-ULB's own SubWaterFlux.m) rather than the local one – see its gradient_convention type parameter. Kept for direct comparison against Kori-ULB's Wd/SHAKTI's laminar sheet closure, but physically inconsistent with K24's own turbulent-flow assumption for q – not the default, and not recommended as \"the\" reported thickness for a K24 run. Clamped to [Wmin, Wmax].\n\nBoth DarcyWeisbachThickness and LaminarThickness take an AbstractGradientConvention (see below) as a type parameter, the same trait-dispatch pattern as AbstractDissipationMelt/AbstractPsiOutAlgorithm above, rather than a plain Bool field: the local-gradient and domain-mean-gradient cases have a genuinely different type for the gradient term (a Field vs a bare scalar) in the broadcast each closure runs, so branching on a runtime Bool field would leave that a small Union type and risk keeping the @. broadcast from being fully specialized. Baking the choice into the algorithm's own type instead – so it's already fixed by the time typeof(model) is known, not read from a field at runtime – means update_W! dispatches directly to a concretely-typed method body, no extra conversion step needed.\n\n\n\n\n\n","title":"FastHydrology.AbstractWaterThicknessAlgorithm"},{"category":"type","location":"API_public/#FastHydrology.ArealConduitThickness","page":"API Reference","text":"Report model.S_inf / model.l_c (the conduit cross-section smeared over the inter-conduit spacing) as state.W. See AbstractWaterThicknessAlgorithm's docstring for the full comparison.\n\n\n\n\n\n","title":"FastHydrology.ArealConduitThickness"},{"category":"type","location":"API_public/#FastHydrology.DarcyWeisbachThickness","page":"API Reference","text":"Report the turbulent Darcy-Weisbach sheet-flow inversion (f*rho_w*q^2 / (4*abs_grad_phi0))^(1/3) as state.W, clamped to [Wmin, Wmax]. KazmierczakHydroModel's default water_thickness_algorithm (with gradient_convention = MeanGradient()). See AbstractWaterThicknessAlgorithm's docstring for the full comparison, and AbstractGradientConvention's docstring for what gradient_convention = LocalGradient() does here instead.\n\n\n\n\n\n","title":"FastHydrology.DarcyWeisbachThickness"},{"category":"type","location":"API_public/#FastHydrology.LaminarThickness","page":"API Reference","text":"Report the original laminar Le Brocq/Weertman sheet-flow inversion (12*eta_w*q / abs_grad_phi0_s)^(1/3) as state.W, clamped to [Wmin, Wmax], with gradient_convention = MeanGradient() by default (matching Kori-ULB's own SubWaterFlux.m). See AbstractWaterThicknessAlgorithm's docstring for why this closure is kept but not the default, and AbstractGradientConvention's docstring for what gradient_convention = LocalGradient() does here.\n\n\n\n\n\n","title":"FastHydrology.LaminarThickness"},{"category":"type","location":"API_public/#FastHydrology.AbstractGradientConvention","page":"API Reference","text":"Trait selecting which gradient value DarcyWeisbachThickness/LaminarThickness divide by: the local, per-cell gradient (LocalGradient) or a single domain-wide masked mean (MeanGradient). See AbstractWaterThicknessAlgorithm's docstring for why this is a type parameter rather than a Bool field.\n\n\n\n\n\n","title":"FastHydrology.AbstractGradientConvention"},{"category":"type","location":"API_public/#FastHydrology.LocalGradient","page":"API Reference","text":"Use the local, per-cell gradient magnitude – matches the convention update_S_inf! already uses. LaminarThickness's alternative to its own default (MeanGradient); available on DarcyWeisbachThickness too, as the alternative to its own default (also MeanGradient, chosen for consistency between the two closures – see MeanGradient's docstring), for callers who want the locally-varying-gradient convention that's the more usual physical choice for a turbulent closure.\n\n\n\n\n\n","title":"FastHydrology.LocalGradient"},{"category":"type","location":"API_public/#FastHydrology.MeanGradient","page":"API Reference","text":"Use a single domain-wide masked mean of the gradient magnitude, in place of its local, per-cell value. LaminarThickness's default, matching Kori-ULB's own SubWaterFlux.m (mean(gdsmag(...))); also DarcyWeisbachThickness's default, for consistency between the package's two sheet-flow closures (both then use the same averaging convention, differing only in laminar-vs-turbulent physics) – pass gradient_convention = LocalGradient() to either if you want the locally-varying gradient instead (the more usual physical choice for a turbulent closure specifically, since a domain-wide mean has no particular physical justification there the way it does, by construction, for reproducing Kori-ULB's own laminar code).\n\n\n\n\n\n","title":"FastHydrology.MeanGradient"},{"category":"function","location":"API_public/#FastHydrology.update_N!","page":"API Reference","text":"update_N!(\n    model::KazmierczakHydroModel,\n    grid::AbstractHydroGrid,\n    state::HydroState\n)\n\n\nUpdate the effective pressure N across the grid using a complementary error function transition between geometric potential and far-field effective pressure.\n\n\n\n\n\nupdate_N!(\n    model::HABHydroModel,\n    grid::AbstractHydroGrid,\n    state::HydroState\n)\n\n\nUpdate the effective pressure N across the grid using a complementary error function transition between geometric potential and far-field effective pressure.\n\n\n\n\n\n","title":"FastHydrology.update_N!"},{"category":"function","location":"API_public/#FastHydrology.update_Po!","page":"API Reference","text":"update_Po!(\n    model::AbstractHydroModel,\n    grid::AbstractHydroGrid,\n    state::HydroState\n)\n\n\nUpdate the hydrostatic ice overburden pressure Po based on ice thickness h. Defined for all models that carry a Po field and share the same rho_i, g constants.\n\n\n\n\n\n","title":"FastHydrology.update_Po!"},{"category":"function","location":"API_public/#FastHydrology.update_H!","page":"API Reference","text":"update_H!(\n    model::KazmierczakHydroModel,\n    grid::AbstractHydroGrid\n)\n\n\nUpdate the conduit thickness H by calculating separate values for hard and soft beds, then interpolating based on the bed heterogeneity indicator kappa.\n\n\n\n\n\n","title":"FastHydrology.update_H!"},{"category":"function","location":"API_public/#FastHydrology.update_S_inf!","page":"API Reference","text":"update_S_inf!(\n    model::KazmierczakHydroModel,\n    grid::AbstractHydroGrid\n)\n\n\nUpdate the far-field conduit cross-sectional area S_inf using the Manning or Gauckler-Manning-Strickler flow law.\n\n\n\n\n\n","title":"FastHydrology.update_S_inf!"},{"category":"function","location":"API_public/#FastHydrology.update_N_inf!","page":"API Reference","text":"update_N_inf!(\n    model::KazmierczakHydroModel,\n    grid::AbstractHydroGrid\n)\n\n\nUpdate the far-field effective pressure N_inf based on conduit geometry and basal velocity, constrained by ice overburden pressure limits.\n\n\n\n\n\n","title":"FastHydrology.update_N_inf!"},{"category":"function","location":"API_public/#FastHydrology.update_Q!","page":"API Reference","text":"update_Q!(\n    model::KazmierczakHydroModel,\n    grid::AbstractHydroGrid\n)\n\n\nUpdate the volumetric water flux per conduit Q by scaling the distributed flux q by the characteristic channel spacing l_c.\n\n\n\n\n\n","title":"FastHydrology.update_Q!"},{"category":"function","location":"API_public/#FastHydrology.update_p_w!","page":"API Reference","text":"update_p_w!(\n    model::HABHydroModel,\n    grid::AbstractHydroGrid,\n    state::HydroState\n)\n\n\nUpdate the water pressure p_w.\n\n\n\n\n\n","title":"FastHydrology.update_p_w!"},{"category":"type","location":"API_public/#FastHydrology.AbstractDrainageMode","page":"API Reference","text":"Trait controlling which opening terms update_N_inf! includes in Eq. (5b)/(6a) of Kazmierczak et al 2024, and which Q_c update_H! uses in Eq. (9) – i.e. whether the model is left free to switch between efficient and inefficient drainage (the paper's default behaviour) or is forced to produce one or the other throughout, as in the paper's Sect. 3.2. Stored as a type parameter and resolved by multiple dispatch, the same pattern as AbstractDissipationMelt and AbstractPsiOutAlgorithm above.\n\nBothDrainage() (default): both the sliding-over-obstacles opening term (||vb||hb, inefficient) and the melt-driven opening term (Qw||grad(phi)||/(rhoi*Lw), efficient) are included in Eq. (5b), and `updateH!uses the model's configuredQ_c` unchanged – the paper's normal automatic switch between drainage types based on the local water flux.\nEfficientOnly(): the sliding opening term is dropped from Eq. (5b)/(6a), and update_H! is given Q_c = 0 so that Eq. (9)'s exp(-Q/Qc) -> 0 for any Q > 0, forcing the soft-bed conduit geometry to the canal/efficient case (Hsoft -> H_0) everywhere.\nInefficientOnly(): the melt opening term is dropped from Eq. (5b), and update_H! is given Q_c = Inf so that exp(-Q/Qc) -> 1, forcing the soft-bed conduit geometry to the inter-clastic- film/inefficient case (Hsoft -> sqrt(Sinf)/Ftill) everywhere.\n\nNote the Qc -> 0 / Qc -> Inf assignment above is the corrected version of the paper's own Sect. 3.2 description (as published, it prescribes Qc = Inf for the entirely-efficient case and Qc = 0 for the entirely-inefficient case); the authors confirmed by email that this is a typo and the two are swapped, which also matches Eq. (9) directly – the exp(-Q/Q_c) limits above only work out this way around.\n\n\n\n\n\n","title":"FastHydrology.AbstractDrainageMode"},{"category":"function","location":"API_public/#FastHydrology.load_Kazmierczak","page":"API Reference","text":"load_Kazmierczak(\n    path::String;\n    bed_rheology\n) -> Tuple{Any, Any, Tuple{Any, Any}, Tuple{Any, Any}, Vararg{Any, 9}}\n\n\nLoad a .mat file from the Kazmierczak et al. (2024) Thwaites hydrology output and return the processed fields needed for the simulation.\n\nArguments\n\npath::String: path to the .mat file.\nbed_rheology: bed hardness rule used to compute κ (:hard, :soft, :mixed, or :mixed_smooth; default :hard).\n\nReturns\n\nNx, Ny: grid dimensions.\nxlims, ylims: grid extent in meters (cell-centered, with half-cell padding to extend to grid edge faces).\nmask: grounded ice mask.\nh: ice thickness (m).\nb: bed elevation (m).\nabs_v_b: basal velocity magnitude (m/s).\nA_visc: viscocity parameter in Glen's flow law.\nG: geothermal heat flux (W m⁻²), for KazmierczakHydroModel's G_in/q_T_in constructor (Eq. 3 of Kazmierczak et al 2024).\nq_T: conductive heat flux into the ice at the bed (W m⁻²). The source file has no field for this term (only relevant for cold-based ice), so it is returned as zero everywhere – pass a real field yourself if your bed isn't uniformly temperate.\nṁ: complete basal melt rate per unit area (kg m⁻² s⁻¹), i.e. the source model's own converged Eq. 3 output (already includes its own frictional-heating and, likely, dissipation terms) – for KazmierczakHydroModel's mdot_in constructor, normally paired with sliding_law = NoSlidingLaw().\nκ: bed hardness (0: hard, 1: soft).\n\n\n\n\n\n","title":"FastHydrology.load_Kazmierczak"},{"category":"function","location":"API_public/#FastHydrology.load_yelmox","page":"API Reference","text":"load_yelmox(\n    path::String;\n    bed_rheology\n) -> Tuple{Any, Any, Tuple{Any, Any}, Tuple{Any, Any}, Vararg{Any, 9}}\n\n\nLoad an NCDatasets file from the yelmox and return the processed fields needed for the simulation of the Kazmierczak et al 2024 hydrology model.\n\nArguments\n\npath::String: path to the .nc file.\nbed_rheology: bed hardness rule used to compute κ (:hard, :soft, :mixed, or :mixed_smooth; default :mixed_smooth).\n\nReturns\n\nNx, Ny: grid dimensions.\nxlims, ylims: grid extent in meters (cell-centered, with half-cell padding to extend to grid edge faces).\nmask: grounded ice mask.\nh: ice thickness (m).\nb: bed elevation (m).\nabs_v_b: basal velocity magnitude (m/s).\nA_visc: viscocity parameter in Glen's flow law.\nG: geothermal heat flux (W m⁻²), for KazmierczakHydroModel's G_in/q_T_in constructor (Eq. 3 of Kazmierczak et al 2024).\nq_T: conductive heat flux into the ice at the bed (W m⁻²), Yelmo's own Q_ice_b, for the same constructor.\nṁ: complete basal melt rate per unit area (Kg m⁻² s⁻¹), i.e. Yelmo's own converged basal mass balance (already includes its own frictional-heating term Q_b) – for KazmierczakHydroModel's mdot_in constructor, normally paired with sliding_law = NoSlidingLaw().\nκ: bed hardness (0: hard, 1: soft).\n\n\n\n\n\n","title":"FastHydrology.load_yelmox"},{"category":"function","location":"API_public/#FastHydrology.compute_lims","page":"API Reference","text":"compute_lims(\n    xc::AbstractArray,\n    yc::AbstractArray\n) -> Tuple{Tuple{Any, Any}, Tuple{Any, Any}}\n\n\nCompute the cell-centered grid limits with half-cell padding from coordinate vectors.\n\nArguments\n\nxc::AbstractArray: the x values of the grid cell centers.\nyc::AbstractArray: the y values of the grid cell centers.\n\n\n\n\n\n","title":"FastHydrology.compute_lims"},{"category":"function","location":"API_public/#FastHydrology.perYear2perSecond","page":"API Reference","text":"perYear2perSecond(f::Real) -> Any\n\n\nConvert input float f from per year to per second.\n\n\n\n\n\n","title":"FastHydrology.perYear2perSecond"},{"category":"function","location":"API_public/#FastHydrology.perSecond2perYear","page":"API Reference","text":"perSecond2perYear(f::Real) -> Any\n\n\nConvert input float f from per second to per year.\n\n\n\n\n\n","title":"FastHydrology.perSecond2perYear"},{"category":"function","location":"API_public/#FastHydrology.Km2m","page":"API Reference","text":"Km2m(f::Real) -> Any\n\n\nConvert input float f from kilometers to meters.\n\n\n\n\n\n","title":"FastHydrology.Km2m"},{"category":"function","location":"API_public/#FastHydrology.visualize_grid","page":"API Reference","text":"Visualize the corners of a hydrology grid, showing cell centers and boundaries. Implemented by the FastHydrologyMakieExt package extension – see visualize_field for why this is a stub here.\n\n\n\n\n\n","title":"FastHydrology.visualize_grid"},{"category":"function","location":"API_public/#FastHydrology.visualize_field","page":"API Reference","text":"Visualize a field. Implemented by the FastHydrologyMakieExt package extension, active once a Makie backend (e.g. CairoMakie) is loaded alongside FastHydrology (using FastHydrology, CairoMakie). Defined here as a stub (no methods) so the name is owned by FastHydrology and the extension can add methods to it, mirroring how step! in run.jl is owned by FastHydrology and extended by FastHydrologyShaktiExt.\n\n\n\n\n\n","title":"FastHydrology.visualize_field"},{"category":"function","location":"API_public/#FastHydrology.mask_field","page":"API Reference","text":"mask_field(\n    field::Oceananigans.Fields.Field,\n    mask,\n    value\n) -> Oceananigans.Fields.Field\n\n\nSet a field to a given input value where the mask is not 1. Does not touch plotting/Makie at all (just field indexing), so unlike visualize_field/visualize_grid this has a real implementation here rather than living behind the FastHydrologyMakieExt extension.\n\n\n\n\n\n","title":"FastHydrology.mask_field"},{"category":"section","location":"examples/ShaktiCoupling/#ShaktiCoupling","page":"Coupling to Shakti.jl","text":"This is an example of how to use the Shakti.jl subglacial hydrology solver (Sommers et al. 2018, https://gmd.copernicus.org/articles/11/2955/2018/) from within FastHydrology.jl, via the ShaktiHydroModel/TimeSimulation combination provided by the FastHydrologyShaktiExt package extension. Unlike KazmierczakHydroModel/HABHydroModel above, Shakti is a genuinely time-evolving solver with its own grid, state, and Picard-iteration solve for the hydraulic head at every timestep – FastHydrology just wraps it and delegates to it, rather than reimplementing any of its physics.\n\nThe extension activates automatically once both packages are loaded, so the only extra step compared to the other examples is using Shakti alongside using FastHydrology.\n\nNote: FastHydrology and Shakti both export a function called run! (and step!) – unrelated generic functions that happen to share a name. Loading both packages makes the bare name ambiguous, so this example always calls FastHydrology.run!/FastHydrology.step! qualified.\n\nusing FastHydrology\nusing Shakti\nusing CairoMakie","title":"Coupling to Shakti.jl"},{"category":"section","location":"examples/ShaktiCoupling/#A-minimal-Shakti-simulation","page":"Coupling to Shakti.jl","text":"Shakti owns its own grid/state, independent of FastHydrology's AbstractHydroGrid/HydroState. Here we build a small, synthetic domain: a sloped bed, mixed grounded/ocean/land/other-basin boundary cells, and a single point-source moulin feeding meltwater in at one interior cell.\n\nnx, ny = 10, 10\ngrid  = Shakti.Grid(nx, ny, 1e4, 1e4)\nstate = Shakti.State(grid)\np     = Shakti.ModelParameters(e_v = 0.0)\nmi    = Shakti.ConstantMeltInput()\nsl    = Shakti.RegularizedCoulombSlidingLaw(0.25)\n\nmask = fill(Shakti.GROUNDED, nx, ny)\nmask[end, :] .= Shakti.OCEAN\nmask[1, :]   .= Shakti.LAND\nmask[:, 1]   .= Shakti.OTHER_BASIN\n\nA_visc = fill(5e-25, nx, ny)\nzb     = repeat(reshape(-0.02 .* grid.x, nx, 1), 1, ny) # sloped bed\nzs     = zb .+ 1000.0\nb      = fill(0.01, nx, ny) # initial gap height\nG      = fill(0.06, nx, ny) # geothermal heat flux\nub_x   = fill(1e-6, nx + 1, ny)\nub_y   = zeros(nx, ny + 1)\nieb    = zeros(nx, ny)\nieb[5, 5] = 3 / (grid.dx * grid.dy) # a single moulin\ntaub_x = zeros(nx + 1, ny)\ntaub_y = zeros(nx, ny + 1)\n\nShakti.set_initial_conditions!(state, grid, p, mi, sl, mask, A_visc, zb, zs, b, G, ub_x, ub_y, ieb, taub_x, taub_y)\n\nA direct (Cholesky) solve backs the elliptic head scheme's Picard iteration.\n\nls = Shakti.CholeskyDirectSolver(grid)\nps = Shakti.PicardSolver(500, 1e-6, ls, grid)","title":"A minimal Shakti simulation"},{"category":"section","location":"examples/ShaktiCoupling/#Wrapping-it-as-a-ShaktiHydroModel","page":"Coupling to Shakti.jl","text":"Shakti.Simulation bundles the grid, state, model parameters, and every scheme choice (head/gap/sliding-law/...) together – ShaktiHydroModel just wraps that as an AbstractHydroModel, and TimeSimulation wraps the model, exactly like SteadyStateSimulation wraps KazmierczakHydroModel/HABHydroModel above.\n\ndt = 3600.0 # 1 hour\ntsteps = 6\nshakti_sim = Shakti.Simulation(grid, state, tsteps, dt, p, \"implicit\", String[], mi, sl; ps = ps)\n\nmodel    = ShaktiHydroModel(shakti_sim)\ntime_sim = TimeSimulation(model)","title":"Wrapping it as a ShaktiHydroModel"},{"category":"section","location":"examples/ShaktiCoupling/#Running-the-whole-simulation","page":"Coupling to Shakti.jl","text":"FastHydrology.run! delegates straight to Shakti.run!, which advances all tsteps timesteps – this is the same as calling Shakti.run!(shakti_sim) directly.\n\nFastHydrology.run!(time_sim)\n\nfig, ax, hm = heatmap(Array(shakti_sim.state.N); axis = (; title = \"Effective pressure N [Pa]\"))\nColorbar(fig[1, 2], hm)\nfig","title":"Running the whole simulation"},{"category":"section","location":"examples/HAB/#HAB","page":"Height above buoyancy (HAB)","text":"This is an example of how to run the FastHydrology.jl package for the steady state problem of the HAB model, described in Sec. 2.1.1 of Kazmierczak et al 2022 (https://doi.org/10.5194/tc-16-4537-2022).\n\nThe figure below comes from running this model on the same Thwaites Amundsen-sector dataset used in the Kazmierczak et al 2024 example.","title":"Height above buoyancy (HAB)"},{"category":"section","location":"examples/HAB/#Reproducing-this-figure","page":"Height above buoyancy (HAB)","text":"As in the Kazmierczak et al 2024 example, this needs THWAITES2km_m3_HAB_toto.mat placed at docs/src/examples/input/Kazmierczak2024/. Run the script below directly with julia (it is shown for reference only – the docs build does not execute it):\n\nusing FastHydrology\nusing CairoMakie\n\nT = Float64\npath = joinpath(@__DIR__, \"input\", \"Kazmierczak2024\", \"THWAITES2km_m3_HAB_toto.mat\")\nNx, Ny, xlims, ylims, mask, h, b, abs_v_b, A_visc, G, q_T, ṁ, κ = load_Kazmierczak(path)\n\n# Prepare a grid using the Oceananigans rectilinear grid, and visualize it.\ngrid = OGRectHydroGrid(Nx, Ny, xlims, ylims; T = T)\nfig = visualize_grid(grid)\n\n# Build the model using the data from the input file above. The model holds its model-specific\n# constants and fields, with the rest of the fields common to all models stored in the HydroState\n# below.\nmodel = HABHydroModel(grid)\nstate = HydroState(grid, mask, h, b)\nsim   = SteadyStateSimulation(model, grid, state)\nrun!(sim)\n\n# Rescale for plotting.\nstate.N .*= 1e-6 # makes N [MPa]\nstate.N .= mask_field(state.N, state.mask, NaN)\n\nfig_N = visualize_field(state.N; plot_title = \"N\", transpose_data = true, colorrange = (0, 10))","title":"Reproducing this figure"},{"category":"section","location":"examples/HAB/#Result","page":"Height above buoyancy (HAB)","text":"Effective pressure N [MPa]:\n\n(Image: Effective pressure)\n\nnothing #hide","title":"Result"},{"category":"section","location":"#FastHydrology.jl","page":"FastHydrology.jl","text":"A general Julia framework for simulating subglacial hydrology beneath ice sheets. FastHydrology.jl provides shared grid, state, and simulation infrastructure that different subglacial hydrology models plug into, computing the effective pressure at the base of an ice sheet – the quantity that couples subglacial water to basal sliding. It can be run standalone or coupled into a larger ice-sheet model such as Yelmo/yelmox.","title":"FastHydrology.jl"},{"category":"section","location":"#Models","page":"FastHydrology.jl","text":"Two models are implemented so far, sharing the same grid/state/simulation infrastructure:\n\nKazmierczakHydroModel – the fast, simplified subglacial hydrology model of Kazmierczak et al. 2024. It routes the distributed subglacial water flux over the geometric potential following Le Brocq et al. 2009 (A subglacial water-flow model for West Antarctica, Journal of Glaciology 55(193):879-888), matches that flux to a local conduit scale, and parameterizes the effective pressure for both hard and soft beds with an automatic switch between efficient (channelized) and inefficient (distributed) drainage.\nFlux routing – three interchangeable implementations; see Water Flux for the psi_out_algorithm keyword (RecursivePsiOut()/IterativePsiOut()/TopologicalPsiOut()) and the trade-offs between them.\nWater layer thickness – the reportable state.W has three selectable closures; see Water Flux for the water_thickness_algorithm keyword (DarcyWeisbachThickness()/ArealConduitThickness()/LaminarThickness()) and the gradient_convention the two sheet-flow closures additionally take.\nMelt source – includes, by default, the dissipation melt rate generated by the flux itself as it moves down the hydraulic gradient (a term the paper drops as negligible but which is kept here for generality; toggle with the dissipation_melt keyword), and can also include a frictional-heating term from basal sliding (Eq. 3 of the paper) via the sliding_law keyword – see Sliding Laws, two of whose laws (PowerPlasticSlidingLaw, RegularizedCoulombSlidingLaw) are written to match the sliding laws implemented in Yelmo.jl for coupled ice-dynamics use.\nDrainage mode – automatically switches between efficient (channelized) and inefficient (distributed) drainage by default, based on the local water flux; can instead be forced to one regime throughout the domain via the drainage_mode keyword (AbstractDrainageMode and its subtypes BothDrainage/ EfficientOnly/InefficientOnly), reproducing Sect. 3.2 of the paper.\nDefaults – numerical-safety bounds (Wmin/Wmax/q_max/sigmat) default to no-op values (nothing is clamped unless you ask for it), with KORI-ULB's own values available as explicit opt-ins. longcoupwater has no such safe no-op default and emits a warning explaining how to choose it if left unspecified.\nHABHydroModel – the simpler \"height above buoyancy\" parameterization from Sec. 2.1.1 of Kazmierczak et al. 2022, which assumes a direct hydraulic connection to the ocean at the grounding line.\n\nBoth are steady-state models: subglacial hydrology is assumed to equilibrate on a timescale much shorter than ice-sheet evolution, so effective pressure is recomputed from the current ice geometry and melt rate at each call rather than time-stepped.\n\nA third, genuinely time-evolving model is available as an optional extension:\n\nShaktiHydroModel – wraps the separate Shakti.jl package, a solver for the SHAKTI subglacial hydrology model (Sommers et al. 2018, https://gmd.copernicus.org/articles/11/2955/2018/), which transitions smoothly between distributed and channelized drainage rather than treating them as separate regimes. FastHydrology just wraps a Shakti.Simulation and delegates to it; see Coupling to Shakti.jl for a runnable example. Since Shakti pulls in a much heavier set of dependencies (CUDA, Metal, AlgebraicMultigrid, ...), it's loaded via a Julia package extension (FastHydrologyShaktiExt) rather than as a hard dependency – ShaktiHydroModel only becomes usable once you using Shakti alongside using FastHydrology.","title":"Models"},{"category":"section","location":"#Installation","page":"FastHydrology.jl","text":"using Pkg\nPkg.add(url=\"https://github.com/TakisAngelides/FastHydrology.jl\")","title":"Installation"},{"category":"section","location":"#Quick-start","page":"FastHydrology.jl","text":"using FastHydrology\n\n# 1. Build a rectilinear grid. ArrayHydroGrid (plain Arrays, no Oceananigans) is a drop-in swap --\n#    see [Plain-array grid](@ref ArrayGrid).\ngrid = OGRectHydroGrid(Nx, Ny, xlims, ylims)\n\n# 2. Build a model, providing the model-specific input fields.\nmodel = KazmierczakHydroModel(grid, kappa, abs_v_b, A_visc, mdot)\n# or: model = HABHydroModel(grid)\n\n# 3. Build the state, holding fields common to every model (grounded-ice mask, ice thickness,\n#    bedrock elevation, and the outputs: effective pressure N and water layer thickness W).\nstate = HydroState(grid, mask, h, b)\n\n# 4. Run the steady-state solve.\nsim = SteadyStateSimulation(model, grid, state)\nrun!(sim)\n\n# state.N and state.W (and, for KazmierczakHydroModel, model.q) now hold the solution.\n\nSee the full API Reference for every exported type and function, including their arguments, units, and governing equations.","title":"Quick start"},{"category":"section","location":"#Examples","page":"FastHydrology.jl","text":"Four examples are included. Coupling to Shakti.jl and Plain-array grid run on small synthetic geometry and execute live as part of the docs build. Kazmierczak et al 2024 and Height above buoyancy (HAB) run against real Thwaites Glacier data instead; that dataset (~90 MB) isn't shipped with the repository, so those two pages render pre-generated figures from a real run, with the full data-loading/model/run pipeline included as reference code and instructions for reproducing it yourself:\n\nKazmierczak et al 2024\nHeight above buoyancy (HAB)\nCoupling to Shakti.jl\nPlain-array grid (no Oceananigans)","title":"Examples"},{"category":"section","location":"#Checkpointing-and-periodic-output","page":"FastHydrology.jl","text":"A coupled driver (e.g. an ice-flow model calling run! on a SteadyStateSimulation once per iteration of its own timestep loop) owns the only loop in that pattern – FastHydrology's own models are steady-state and have no internal loop of their own (see Models above), so periodic output/checkpointing is driven from the caller's loop, not from inside FastHydrology:\n\nwriter = NetCDFOutputWriter(output_path, grid, [:N, :W])\n\nfor k in 1:n_steps\n    # ... update sim.state from e.g. an ice-flow model's current geometry ...\n    run!(sim)\n    write_output!(writer, k, elapsed_time, (N = sim.state.N, W = sim.state.W))\n    if k % checkpoint_every == 0\n        save_checkpoint(checkpoint_path, sim.state, k, elapsed_time)\n    end\nend\nclose_output!(writer)\n\nwrite_output! appends one time-slice to a NetCDF file with an unlimited time dimension and flushes it to disk immediately, so a run killed mid-loop (e.g. a SLURM job hitting its wall-time limit) leaves every slice written before the kill intact and readable, instead of losing everything the way writing a single result file at the very end does. save_checkpoint/ load_checkpoint! separately save/restore a full AbstractHydroState's fields, so a restarted job can resume the driver's own loop at the right step/time; pass resume_step to NetCDFOutputWriter when reopening an output file after a restart, to drop any slices the crashed run wrote past the last checkpoint actually resumed from (avoiding duplicates once the loop replays those steps). See Output & Checkpointing for the full API, including why this is a manually-driven utility rather than a checkpoint_every keyword on run! itself – TimeSimulation's only model (ShaktiHydroModel) already gets this from Shakti.run! directly.","title":"Checkpointing and periodic output"},{"category":"section","location":"#Package-structure","page":"FastHydrology.jl","text":"The package is organized around four abstractions:\n\nAbstraction Purpose Concrete implementation(s) provided\nAbstractHydroGrid Grid geometry and the backend-specific glue (field allocation, halo filling, and a few array operations physics code needs without knowing the backend) OGRectHydroGrid (wraps an Oceananigans.RectilinearGrid), ArrayHydroGrid (plain Arrays, no Oceananigans) – any grid backend can implement this interface\nAbstractHydroModel Model-specific constants and fields KazmierczakHydroModel, HABHydroModel, ShaktiHydroModel\nAbstractHydroState Fields common to every model: mask, ice thickness, bedrock elevation (inputs), effective pressure and water thickness (outputs) HydroState (not used by ShaktiHydroModel, which brings its own state)\nAbstractSimulation How to run a model SteadyStateSimulation, TimeSimulation\n\nPhysics code (water_flux.jl, effective_pressure.jl) is written against the grid interface – grid.Nx, grid.dx, alloc_field, fill_halo!, convolve!, minus_gradient_x!, minus_gradient_y!, masked_mean, masked_max_abs, masked_max_abs_diff, overwrite_where! – rather than reaching into a specific backend's internals directly, so a different grid backend would only need to implement that interface. Most of these already have a default implementation that works for any grid whose fields behave like plain arrays, which is why ArrayHydroGrid only needs to implement alloc_field.","title":"Package structure"},{"category":"section","location":"#Source-layout","page":"FastHydrology.jl","text":"src/ is split into common/ (infrastructure shared by every model) and models/<name>/ (one folder per hydrology model), so it's clear at a glance which files are shared and which files a new model needs to add.\n\nsrc/common/ – shared infrastructure, touched by every model:\n\ngrid.jl – the grid abstraction and its OGRectHydroGrid/ArrayHydroGrid implementations.\noperations.jl – registers min, max, erf as broadcastable Oceananigans field operations.\nfft_convolution.jl – a cached, plan-reusing FFT convolution (used by the stress-gradient coupling smoothing in models/kazmierczak2024/water_flux.jl) that avoids ImageFiltering.jl's per-call allocation. grid.jl's convolve! passes it interior(...) views of the Oceananigans Fields directly (not Field.data, which carries halo padding cached_fft_convolve! would otherwise mistake for real domain) so there is no extra copy either.\nmodel.jl – just abstract type AbstractHydroModel end, the type every model subtypes.\nstate.jl – HydroState.\nsimulation.jl – AbstractSimulation, SteadyStateSimulation, TimeSimulation.\neffective_pressure.jl – update_Po!, the one effective-pressure step shared by every model that carries a Po field with the same rho_i/g constants.\nrun.jl – run!(::SteadyStateSimulation) and the step! stub (step! has no methods in core – see below).\ncheckpoint.jl – save_checkpoint/load_checkpoint!, saving/restoring an AbstractHydroState's fields (as NetCDF) for a coupled driver's own loop to resume from.\noutput.jl – AbstractOutputWriter/NetCDFOutputWriter, writing named fields to a NetCDF file one time-slice at a time (unlimited time dimension, flushed to disk on every write), with resume_step support for continuing an existing file after a restart.\nutilities.jl – unit conversions and grid-geometry helpers.\nplotting.jl – mask_field, plus stub declarations for visualize_field/visualize_grid (their implementations live in ../ext/FastHydrologyMakieExt.jl, see below).\n\nsrc/models/kazmierczak2024/ – the Kazmierczak et al. 2024 model:\n\nmodel.jl – KazmierczakHydroModel and its constructors, plus the trait types its keywords select between (AbstractDissipationMelt, AbstractPsiOutAlgorithm, AbstractWaterThicknessAlgorithm, AbstractGradientConvention, AbstractDrainageMode). KazmierczakHydroModel itself composes KazmierczakParams (physical constants/solver config) and KazmierczakWorkspace (array buffers) behind a transparent model.<field> interface, rather than one struct with both flattened together.\nsliding_law.jl – AbstractSlidingLaw and its subtypes, plus calc_tau_b/update_tau_b!, turning a sliding law and the current effective pressure/sliding velocity into a basal shear stress.\nwater_flux.jl – geometric potential, flux routing, and water layer thickness.\neffective_pressure.jl – update_N!, update_H!, update_S_inf!, update_N_inf!, update_Q!.\nrun.jl – update_steady_state!(::KazmierczakHydroModel, ...).\ndata_loaders.jl – load_Kazmierczak and load_yelmox, for reading .mat/.nc input data into the arrays the constructor above expects.\n\nsrc/models/hab/ – the height-above-buoyancy model:\n\nmodel.jl – HABHydroModel and its constructor.\neffective_pressure.jl – update_N!, update_p_w!.\nrun.jl – update_steady_state!(::HABHydroModel, ...).\n\nsrc/models/shakti/ – the ShaktiHydroModel wrapper:\n\nmodel.jl – just the ShaktiHydroModel wrapper struct (defined unconditionally, no dependency on Shakti itself). The methods that make it actually runnable (run!/step!) live in ../../../ext/FastHydrologyShaktiExt.jl instead, since Julia's package-extension mechanism requires extensions to live under ext/ at the package root – see that file's own docstring, and Adding a new model below for the pattern this leaves for a future externally-wrapped model.\n\next/ – package extensions, unconditionally at the package root (Julia's extension mechanism requires this):\n\nFastHydrologyShaktiExt.jl – adds the run!/step! methods that make ShaktiHydroModel actually runnable, active only once Shakti is loaded alongside FastHydrology.\nFastHydrologyMakieExt.jl – adds visualize_field/visualize_grid's implementations, active only once a Makie backend (e.g. CairoMakie) is loaded alongside FastHydrology. Kept as an extension rather than a hard dependency since CairoMakie is one of the slower-compiling packages in the ecosystem and plotting isn't needed to run a simulation.","title":"Source layout"},{"category":"section","location":"#Adding-a-new-model","page":"FastHydrology.jl","text":"To add a new hydrology model X, following the same layout as the existing models:\n\nCreate src/models/x/model.jl defining XHydroModel <: AbstractHydroModel and its constructor(s).\nAdd whichever of these x/ needs, using the same filenames as kazmierczak2024//hab/ so the pattern stays recognizable:\neffective_pressure.jl for an update_N!(model::XHydroModel, ...) method – every steady-state model needs one; common/effective_pressure.jl's update_Po! is already available to reuse if X carries a Po field with the standard rho_i/g constants.\nrun.jl for update_steady_state!(model::XHydroModel, ...) if X is steady-state (see common/run.jl's run!(::SteadyStateSimulation), which dispatches to it), or run!/step! methods on TimeSimulation{XHydroModel} if X is time-evolving.\nwater_flux.jl, sliding_law.jl, data_loaders.jl, or any other file X's own physics needs that doesn't belong in common/.\nIf X wraps an external package via a weak dependency (the way ShaktiHydroModel wraps Shakti.jl), keep model.jl a thin, unconditionally-defined wrapper struct in src/models/x/, and add the methods that make it runnable in a package extension under ext/ (see ext/FastHydrologyShaktiExt.jl for the pattern).\ninclude(...) the new files in src/FastHydrology.jl under a # Model: X banner (see the existing banners for kazmierczak2024/hab/shakti), and add the new exports.\nAdd X's tests under test/models/x/, and include(...) them from test/runtests.jl (see how kazmierczak2024/hab are included there).\nUpdate the table in Models above and this Source layout section.","title":"Adding a new model"},{"category":"section","location":"#Testing","page":"FastHydrology.jl","text":"using Pkg\nPkg.test(\"FastHydrology\")\n\ntest/ mirrors the src/ split above: test/common/ for grid-backend/checkpoint/output tests that exercise the shared infrastructure (using whichever model is a convenient fixture, not necessarily testing that model itself), and test/models/<name>/ for each model's own tests. The suite exercises both steady-state models end-to-end, both data loaders across all bed-rheology options, regression-tests specific bugs found during development, and (in its own module, in test/models/shakti/shakti_ext_test.jl) ShaktiHydroModel's run!/step! extension methods against a real Shakti.Simulation – its own module because FastHydrology and Shakti both export run!, which using both in the same scope would make ambiguous.","title":"Testing"},{"category":"section","location":"#License","page":"FastHydrology.jl","text":"GNU General Public License v3.0 – see LICENSE.","title":"License"},{"category":"section","location":"examples/Kazmierczak2024/#Kazmierczak2024","page":"Kazmierczak et al 2024","text":"This is an example of how to run the FastHydrology.jl package for the steady state problem of Kazmierczak et al 2024 (https://doi.org/10.5194/tc-18-5887-2024).\n\nThe figures below come from running this model on the Thwaites Amundsen-sector dataset used for Fig. S3 of Kazmierczak et al 2024, on a hard bed (bed_rheology = :hard).","title":"Kazmierczak et al 2024"},{"category":"section","location":"examples/Kazmierczak2024/#Reproducing-these-figures","page":"Kazmierczak et al 2024","text":"The input file (THWAITES2km_m3_HAB_toto.mat, ~90 MB) is not shipped with this repository, so that cloning/installing FastHydrology.jl stays lightweight – this page is built from the static figures below rather than by running the pipeline live. To reproduce them yourself:\n\nObtain THWAITES2km_m3_HAB_toto.mat and place it at docs/src/examples/input/Kazmierczak2024/THWAITES2km_m3_HAB_toto.mat.\nRun the script below directly with julia (it is shown for reference only – the docs build does not execute it):\n\nusing FastHydrology\nusing CairoMakie\n\nT = Float64\npath = joinpath(@__DIR__, \"input\", \"Kazmierczak2024\", \"THWAITES2km_m3_HAB_toto.mat\")\nNx, Ny, xlims, ylims, mask, h, b, abs_v_b, A_visc, G, q_T, ṁ, κ = load_Kazmierczak(path; bed_rheology = :hard)\n\n# Prepare a grid using the Oceananigans rectilinear grid, and visualize it.\ngrid = OGRectHydroGrid(Nx, Ny, xlims, ylims; T = T)\nfig = visualize_grid(grid)\n\n# Build the model using the data from the input file above. The model holds its model-specific\n# fields, with the rest of the fields common to all models stored in the HydroState below.\n#\n# By default, the water source feeding the flux-routing algorithm includes the dissipation melt\n# rate generated by the flux itself as it moves down the hydraulic gradient (`dissipation_melt =\n# true`) -- a term Kazmierczak et al 2024 drop as negligible but which is kept here for\n# generality. It can be turned off with `dissipation_melt = false` to reproduce the paper's\n# original water balance exactly. We also silence the per-call Picard convergence printout here\n# with `dissipation_verbose = false`.\n#\n# `longcoupwater` (the stress-gradient-coupling smoothing width) has no safe default that works\n# for every grid resolution, so it must be passed explicitly or `KazmierczakHydroModel` warns and\n# falls back to 5.0 -- see its docstring for how to choose a value from your own grid's resolution\n# relative to ice thickness. 5.0 is appropriate for this dataset's 2 km grid.\n#\n# `state.W`'s closure (`water_thickness_algorithm`, default `DarcyWeisbachThickness()`) and its\n# numerical-safety bounds (`Wmin`/`Wmax`/`q_max`/`sigmat`, all no-op by default -- nothing is\n# clamped unless you ask for it) are left at their defaults here; see the API reference for the\n# other closures (`ArealConduitThickness`, `LaminarThickness`) and KORI-ULB's own bound values.\nmodel = KazmierczakHydroModel(grid, κ, abs_v_b, A_visc, ṁ; longcoupwater = 5.0, dissipation_verbose = false)\nstate = HydroState(grid, mask, h, b)\nsim   = SteadyStateSimulation(model, grid, state)\nrun!(sim)\n\n# Rescale for plotting.\nmodel.q .*= perSecond2perYear(1.0) / 1e4 # makes q [10⁴ m² a⁻¹]\nstate.N .*= 1e-6 # makes N [MPa]\nmodel.q .= mask_field(model.q, state.mask, NaN)\nstate.N .= mask_field(state.N, state.mask, NaN)\nstate.W .= mask_field(state.W, state.mask, NaN)\n\nfig_q = visualize_field(model.q; plot_title = \"Distributed water flux q [10⁴ m² a⁻¹]\", transpose_data = true, colorrange = (0, 10))\nfig_W = visualize_field(state.W; plot_title = \"Water thickness W [m] (DarcyWeisbachThickness)\", transpose_data = true, colorrange = extrema(filter(!isnan, state.W.data)))\nfig_N = visualize_field(state.N; plot_title = \"Effective pressure N [MPa]\", transpose_data = true, colorrange = (0, 10))\n\n# ## Adding a basal sliding law\n#\n# By default `sliding_law = NoSlidingLaw()`, so `mdot_in` alone is assumed to already be the\n# complete melt rate. Passing a pressure-dependent sliding law instead adds the frictional-heating\n# term tau_b*v_b/L_w (Eq. 3 of Kazmierczak et al 2024) on top of it, computed from a basal shear\n# stress `tau_b(N, v_b)`. Since tau_b then depends on the effective pressure N, which is itself\n# downstream of q, `resolve_q!` widens its Picard loop to solve for q and N jointly rather than\n# nesting a second loop around the first -- see the `sliding_law` keyword on `KazmierczakHydroModel`\n# and `resolve_q!`'s docstring for the physics and the loop design.\n#\n# `PowerPlasticSlidingLaw` and `RegularizedCoulombSlidingLaw` are written to match the two sliding\n# laws implemented in Yelmo.jl's `basal_dragging.jl` (`beta_method` 1/2/4 and 3/5 respectively), so\n# a future ice-dynamics coupling can configure both sides with numerically matching laws. The\n# parameters below (`c_till`, `q`, `u0`) are illustrative, not calibrated to Thwaites.\nsliding_law = RegularizedCoulombSlidingLaw(c_till = 0.5, q = 1/3, u0 = perYear2perSecond(100.0))\nmodel_sliding = KazmierczakHydroModel(grid, κ, abs_v_b, A_visc, ṁ;\n                                       sliding_law = sliding_law, longcoupwater = 5.0,\n                                       dissipation_verbose = false, coupling_verbose = false)\nstate_sliding = HydroState(grid, mask, h, b)\nrun!(SteadyStateSimulation(model_sliding, grid, state_sliding))\n\nmodel_sliding.tau_b .*= 1e-6 # makes tau_b [MPa]\nmodel_sliding.tau_b .= mask_field(model_sliding.tau_b, state_sliding.mask, NaN)\nfig_tau_b = visualize_field(model_sliding.tau_b; plot_title = \"Basal shear stress tau_b [MPa]\", transpose_data = true, colorrange = extrema(filter(!isnan, model_sliding.tau_b.data)))\n\n# ## Computing the melt rate faithfully from Eq. 3\n#\n# ṁ above is `load_Kazmierczak`'s `Bmelt` field: KORI-ULB's own converged melt-rate output for this\n# Thwaites run, which already bakes in that model's own frictional-heating estimate. Passing it to\n# `mdot_in` above only stays free of double-counting because `sliding_law = NoSlidingLaw()` is the\n# default -- no frictional term is added by FastHydrology on top of it.\n#\n# If you'd rather have FastHydrology compute the melt rate itself, faithfully to Eq. 3 (background\n# term `(G - q_T)/L_w`, with the frictional-heating and dissipation terms added on top dynamically,\n# same as above), use the other `KazmierczakHydroModel` constructor: `G` and `q_T` (both [W/m^2]) instead\n# of ṁ. `load_Kazmierczak` returns a real geothermal flux field `G`; it has no `q_T` field of its own,\n# so `load_Kazmierczak` returns it as zero everywhere (the usual temperate-bed assumption) -- both are\n# mandatory arguments precisely so that assumption has to be made explicit, not silent.\nmodel_from_G = KazmierczakHydroModel(grid, κ, abs_v_b, A_visc, G, q_T;\n                                      sliding_law = sliding_law, longcoupwater = 5.0,\n                                      dissipation_verbose = false, coupling_verbose = false)\nstate_from_G = HydroState(grid, mask, h, b)\nrun!(SteadyStateSimulation(model_from_G, grid, state_from_G))","title":"Reproducing these figures"},{"category":"section","location":"examples/Kazmierczak2024/#Results","page":"Kazmierczak et al 2024","text":"Distributed water flux q [10⁴ m² a⁻¹]:\n\n(Image: Distributed water flux)\n\nWater thickness W [m]:\n\n(Image: Water thickness)\n\nEffective pressure N [MPa]:\n\n(Image: Effective pressure)\n\nBasal shear stress tau_b [MPa], with RegularizedCoulombSlidingLaw:\n\n(Image: Basal shear stress)\n\nnothing #hide","title":"Results"}]
}
